export { IconCheck as Check, IconTrash as Trash2, IconReceipt as Receipt } from './@tabler/icons-react'

/** A local component, to prove the rule excludes a mark by what it is rather than by its name. */
export declare const OrbitMark: (props: { size?: number }) => unknown
