<?xml version="1.0" encoding="utf-8"?>
<!-- --status-overdue ring, also tinted dynamically from the synced token. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <stroke android:width="2dp" android:color="@color/widget_overdue" />
    <size android:width="22dp" android:height="22dp" />
</shape>
