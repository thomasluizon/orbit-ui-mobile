{
  "thresholdDerivation": {
    "whyThisKeyExists": "lhci parses this file with JSON.parse and reads only ci, lhci, ci:client and ci:server, so this sibling key records the source of the assertions below without reaching lhci.",
    "population": "37 complete batches (185 runs) read from retained .lighthouseci artifacts. apps/web/perf/lhci-hermetic-auth.cjs warms the authenticated route before collection, so the first-request compile of the production server is not measured.",
    "total-blocking-time": "1200 ms. Warm batch medians: mean 792.1 ms, sd 131.4 ms, max 1058.2 ms, so 1200 (just above mean+3sd) fails no clean batch and still fails a regression that adds about 400 ms of main-thread blocking. Re-derive if the warm median moves by more than about 20 percent.",
    "largest-contentful-paint": "Reporting only. On this runner clean and regressed five-run batches overlap (a 500 ms server-render delay passed a 7800 ms median gate), so apps/web/perf/report-lcp.mjs prints all five values and summary statistics without failing the job. Do not restore an LCP gate without a runner whose clean and regressed distributions separate.",
    "resource-summary:script:size": "820000 bytes. Batch medians of script transferSize spanned 749508 to 770185 bytes (2.8 percent), and 820000 is 6.5 percent above the largest, tight enough to catch a real dependency or code-splitting regression.",
    "aggregation": "numberOfRuns 5 with median aggregation for total-blocking-time and script size."
  },
  "ci": {
    "collect": {
      "startServerCommand": "npm run start -w @orbit/web",
      "startServerReadyPattern": "Ready in",
      "startServerReadyTimeout": 120000,
      "url": ["http://127.0.0.1:3000/"],
      "numberOfRuns": 5,
      "puppeteerScript": "apps/web/perf/lhci-hermetic-auth.cjs",
      "settings": {
        "onlyCategories": ["performance"]
      }
    },
    "assert": {
      "aggregationMethod": "median",
      "assertions": {
        "total-blocking-time": ["error", { "maxNumericValue": 1200 }],
        "resource-summary:script:size": ["error", { "maxNumericValue": 820000 }]
      }
    },
    "upload": {
      "target": "filesystem",
      "outputDir": ".lighthouseci"
    }
  }
}
