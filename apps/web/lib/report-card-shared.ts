/** Reports a successful recap-card share so the backend can award the "Show Off" achievement. */
export function reportCardShared(): void {
  // The client report endpoint ships with https://github.com/thomasluizon/orbit-ui-mobile/issues/196 — no-op until then.
}
