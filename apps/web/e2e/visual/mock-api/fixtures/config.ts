import { DEFAULT_CONFIG, type AppConfig } from '@orbit/shared/types/config'

/** The shared default app config is already deterministic; reuse it verbatim. */
export const configFixture: AppConfig = DEFAULT_CONFIG
