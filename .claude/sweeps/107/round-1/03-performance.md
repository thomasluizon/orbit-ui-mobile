# Sweep #3 — Performance (static analysis), issue #107 round 1

Scope: working trees of `orbit-ui-mobile` (main) and `orbit-api` (chore/107-code-health-sweep). Static reading + grep only. Lighthouse / runtime measurement explicitly out of scope (DEF-1, user-owned). Rules cited: issue #107 Strand-2 agent-3 bullets (Web / Mobile / API / Animations), DESIGN.md Motion (line 151) + Bans (line 159, "No `transition-all`. Animate `transform` and `opacity` only"), apps/mobile/CLAUDE.md (Android elevation + overflow bug, line 33).

Format: `SEVERITY · file:line · rule · fix`.

## Web (apps/web)

- MED · `apps/web/app/globals.css:1007-1017` (`.collapsible`, consumed at `apps/web/components/habits/habit-form-fields.tsx:1339`) · CSS transition of `grid-template-rows` — a layout property (DESIGN.md Motion/Bans "transform and opacity only"; #107 agent-3 "No CSS layout-property animations"). Expanding/collapsing the advanced section reflows the whole form subtree every frame for `--dur-base`. Reduced-motion already disables it · replace with a transform/opacity reveal (measure once, animate `translateY`/`opacity` on the revealed block), or record a written deferral if the 0fr→1fr grid trick is accepted as the height-auto idiom.
- LOW · `apps/web/components/tour/tour-tooltip.tsx:328` · `transition-[width,background-color]` animates `width` (layout) on the tour progress dots (DESIGN.md Bans; #107 "Animations: only transform + opacity") · give every dot fixed width and animate `transform: scaleX()` (origin left), or crossfade two sizes with opacity.
- LOW · `apps/web/components/onboarding/onboarding-flow.tsx:325-327` · same `width` transition on onboarding `ProgressDots` (active dot 7→24px) · same `scaleX` fix; keep parity with the mobile fix below.
- LOW · `apps/web/app/globals.css:508-524` (`.loading-bar` + `@keyframes loading-slide`) · infinite animation of `background-position` — not `transform`/`opacity` (DESIGN.md Motion line 151); repaints the gradient for as long as the bar is mounted · move the gradient onto a child/pseudo-element sized 200% and animate `translateX` instead.
- LOW · `apps/web/app/globals.css:662-665` (`@keyframes status-sweep`, `stroke-dashoffset`) + `apps/web/components/goals/goal-detail-drawer.tsx:108` (`transition: 'stroke-dashoffset 280ms'`) + mobile mirror `apps/mobile/components/ui/status-dot.tsx:85-90` (`Animated.timing` on `strokeDashoffset`, necessarily `useNativeDriver: false`, i.e. JS-thread) · SVG `stroke-dashoffset` is neither transform nor opacity (DESIGN.md Motion literal text); paint-only and one-shot, and status-sweep is the canon's own Today completion signature · either document the deviation once in DESIGN.md (sanctioned SVG arc technique) or rebuild the sweep from rotating masked half-discs (transform-only). No layout cost today; flagged for rule consistency, not measured harm.

Checked clean (web): zero `transition-all` anywhere; all other `transition-[...]` arbitrary values name compositor/paint-safe properties (transform/opacity/colors/box-shadow — kit pattern shipped by globals.css itself); all other `@keyframes` (incl. `streak.css`) are transform/opacity; `prefers-reduced-motion` kill switch at `globals.css:882`; no `el.animate()`/WAAPI, no animation libraries; the only continuous rAF path (`components/tour/tour-provider.tsx:268-300`) is event-gated, coalesced, and active only during a tour; no raw `<img>` outside the `LocalImage` seam (`components/ui/local-image.tsx` — blob URLs next/image can't optimize) and `next/image` used for static assets (`components/ui/app-logo.tsx`). Per the brief, `useMemo`/`useCallback` hygiene is left to the React Compiler rules; no compiler-unfixable inline-allocation pattern with a documented stable-reference convention was found to cite.

## Mobile (apps/mobile)

- MED · `apps/mobile/components/navigation/notification-bell.tsx:276-290` · notifications rendered via `visibleNotifications.map(...)` inside a `ScrollView` (#107 agent-3 "FlatList vs `map` render in long lists"; named category: notifications). Server caps the payload at 50 (`orbit-api src/Orbit.Application/Common/AppConstants.cs:30`), so sheet open eagerly mounts up to 50 pressable cards. FlatList is the established in-repo pattern (`components/habit-list.tsx`, `app/chat.tsx`, `components/ui/app-select.tsx`) · render the sheet body with a `FlatList` (actions row as `ListHeaderComponent`).
- MED · `apps/mobile/components/goals/goal-list.tsx:28-45` hosted inside the goals-tab `ScrollView` at `app/(tabs)/index.tsx:1412-1427` · unbounded user-created goals list rendered via `.map()` in a ScrollView (#107 "FlatList vs map"; goals grow exactly like habits, which use FlatList) · convert the goals tab to a `FlatList` with `sharedHeader` as `ListHeaderComponent`.
- MED · `apps/mobile/app/(tabs)/calendar.tsx:454-470` · selected-day habit entries (`filteredEntries.map(...)` — unbounded per-day habit data) rendered inside the screen `ScrollView` (#107 "FlatList vs map"; named category: habits) · move day entries into a `FlatList` (calendar grid as `ListHeaderComponent`) or virtualize the day panel.
- LOW · `apps/mobile/app/achievements.tsx:169-178` + `apps/mobile/app/achievements-sections.tsx:39-50` · achievements catalog mapped inside a `ScrollView` (#107 named category: achievements). Catalog is currently 25 (`orbit-api src/Orbit.Application/Gamification/AchievementDefinitions.cs`) and grows per release; every tile mounts an `Animated.View` wrapper eagerly · acceptable today; switch to a `numColumns` FlatList when the catalog grows, or cap entrance-animation wrappers.
- LOW · `apps/mobile/components/onboarding/onboarding-flow.tsx:49-77` (`ProgressDot`) · `Animated.timing` animating `width` with `useNativeDriver: false` — a JS-thread layout-property animation (DESIGN.md Motion "transform + opacity only"; #107 mobile bullet keeps animation off the JS thread) · animate `transform: [{ scaleX }]` with `useNativeDriver: true` (mirror of the web ProgressDots fix above — land both together per parity).
- MED · `apps/mobile/hooks/use-horizontal-swipe.ts:21-48`, attached at `app/(tabs)/index.tsx:487/1047` and `app/(tabs)/calendar.tsx:163/320` · `PanResponder` runs `onMoveShouldSetPanResponder` on the JS thread for every touch-move on the two hottest screens, including while the habit list is scrolling (#107 "Reanimated worklets stay on the UI thread"; brief: JS-thread work inside gesture handlers that should be worklets) · migrate to RNGH `Gesture.Pan().activeOffsetX(±50).failOffsetY(...)` (+`onEnd` worklet, `runOnJS` only for navigation) — react-native-gesture-handler is already a dependency (`GHFlatList`/DraggableFlatList in habit-list.tsx).
- LOW · `apps/mobile/hooks/use-tour-scroll-container.ts:16-21` with `scrollEventThrottle={16}` at `app/(tabs)/index.tsx:1422`, `app/(tabs)/calendar.tsx:314`, `app/(tabs)/profile.tsx:538`, `components/habit-list.tsx:1774` · always-attached JS `onScroll` streams bridge events at up to 60fps on four hot scroll surfaces to track position for the rarely-active feature tour (brief: JS-thread work inside scroll handlers) · attach the handler only while a tour is active, or track via Reanimated `useAnimatedScrollHandler` + shared value.
- LOW · `apps/mobile/components/message-bubble.tsx:116-121` (chat attachment, 200×192) and `apps/mobile/components/chat/chat-input-area.tsx:141-145` (preview thumb, 64×64 per `app/chat.styles.ts:103-108`) · full-resolution user photos (picked at `quality: 1`, `hooks/use-chat-composer.ts:294`) decoded at native size and GPU-scaled into small thumbs; no `resizeMethod="resize"` anywhere in the app (#107 "image sizing matches display size") · add `resizeMethod="resize"` (or expo-image with explicit sizes) on both, and/or pick at reduced quality/size.

Checked clean (mobile): all backdrop/overlay/celebration animations use `useNativeDriver: true` (confirm-dialog, anchored-menu, toasts, all gamification celebrations, fresh-start, login, chat) — no transparent backdrops on the JS thread; the only `useNativeDriver: false` sites are the two flagged above; `useAnimatedStyle` worklets (`habit-row.tsx:509`, `habit-checklist.tsx:52`) read shared values only — no React state per frame; `progress-bar.tsx` animates `scaleX` native-driven; habit list + chat are FlatList/DraggableFlatList, not nested in ScrollViews; all three `Image` usages have explicit dimensions; `AppLogo` renders the 96px `logo-no-bg.png` at 32dp (correct for 3x density); no same-file elevation-inside-`overflow:'hidden'` combos (apps/mobile/CLAUDE.md line 33) — elevation users (gamification cards, habit-row:540, settings-row:260, trial-expired-modal:126) do not sit in the overflow-hidden files list.

## API (orbit-api)

- HIGH · `src/Orbit.Application/Habits/Commands/BulkLogHabitsCommand.cs:83-90` · per-item awaited heavy call in a loop on a hot endpoint: `foreach (habitId in loggedHabitIds) await GamificationService.ProcessHabitLogged(...)` — each iteration re-runs user `FindOneTrackedAsync` + earned-achievements `FindAsync` + user-today + **all user habits with a 400-day filtered log Include** (+ conditional 90-day and 7-day log scans). Bulk-logging N habits ⇒ ~N×5-6 queries with repeated multi-thousand-row materialization (#107 "EF Core N+1 audit on hot endpoints"; brief "per-item awaited queries inside loops") · make `ProcessHabitLogged` batch-capable: load user/earned/habits+logs once, evaluate achievements for the whole logged set in one pass.
- MED · `src/Orbit.Application/Gamification/Services/GamificationService.cs:40-45` · every single habit-log mutation materializes ALL user habits + 400 days of logs to evaluate achievements; Liftoff/volume need only counts (`CountAsync`/SQL aggregate), perfect day/week/month need ≤31-day windows, the streak metric needs only the logged habit's logs (#107 API bullet "Sync-over-async + allocations" — allocations) · narrow to targeted aggregates and per-check date windows, or project to `(HabitId, Date, Value)` instead of entities.
- MED · `src/Orbit.Application/Habits/Commands/LogHabitCommand.cs:60-63` (`Include(h => h.Logs)` unfiltered) and `:236-241` (`Include(g => g.Habits).ThenInclude(h => h.Logs)` unfiltered) + `src/Orbit.Application/Habits/Commands/BulkLogHabitsCommand.cs:50-53` (`Include(h => h.Logs)` unfiltered) · the single-tap log path loads the habit's entire log history, then every linked goal's every habit's entire history, only to compute current streaks and a same-date duplicate check; the in-repo convention is date-bounded filtered includes (`GetDailySummaryQuery.cs:69`, `GetCalendarMonthQuery.cs:66`, `GamificationService.cs:41-44`, `UserStreakService` 365-day lookback) (brief: missing projection/filtered-include on hot endpoints; #107 allocations) · apply filtered includes bounded by `AppConstants.MaxStreakLookbackDays`-style windows.
- MED · `src/Orbit.Application/Goals/Commands/ReorderGoalsCommand.cs:26-36` · one `FindOneTrackedAsync` round-trip per goal position inside `foreach` — N+1 on every goals drag-reorder; the sibling `ReorderHabitsCommand.cs:23-29` already demonstrates the batched pattern (single `FindTrackedAsync`, loop in memory) · fetch all goals for the position set in one tracked query, then update in memory.
- MED · `src/Orbit.Infrastructure/Persistence/OrbitDbContext.cs:120` vs `src/Orbit.Application/Notifications/Queries/GetNotificationsQuery.cs:29-32` · index/query-shape mismatch on a polled endpoint: list reads `WHERE UserId = @u ORDER BY CreatedAtUtc DESC LIMIT 50` but Notifications has only `(UserId, IsRead)` — every bell fetch sorts the user's full notification set (brief: "indexes vs query shapes — WHERE/ORDER BY columns of hot queries") · add `HasIndex(n => new { n.UserId, n.CreatedAtUtc })` (descending) via migration; keep `(UserId, IsRead)` for the unread `CountAsync`.
- LOW · `src/Orbit.Application/UserFacts/Commands/BulkDeleteUserFactsCommand.cs:27-37` · per-factId `FindOneTrackedAsync` in `foreach` — N+1 on a bulk endpoint; batched precedent at `BulkDeleteHabitsCommand.cs:32` · one `FindTrackedAsync` with `factIds.Contains(f.Id) && f.UserId == ...`, soft-delete in memory.
- LOW · `src/Orbit.Application/Chat/Tools/Implementations/AssignTagsTool.cs:110-135` (`ResolveTagsByNameAsync`) + `src/Orbit.Application/Chat/Tools/Implementations/CreateHabitTool.cs:295-320` (`AssignTagsToHabitAsync`) · per-tag-name `FindOneTrackedAsync` inside a loop — N+1 (N = tag names) on agent tool paths (brief grep-sweep: per-item awaited queries inside loops) · batch-fetch existing tags by the capitalized name set in one query, create the misses.
- LOW · `src/Orbit.Api/Controllers/ConfigController.cs:19-38` · idempotent config-ish GET (limits, feature flags, static sync settings) returns no caching headers — the only Cache-Control in the API is ChatController's no-store (#107 "Caching headers on idempotent reads") · set `Cache-Control: private, max-age=300` (matches the advertised `syncIntervalSeconds: 300`) or `[ResponseCache]`.

Checked clean (API): no sync-over-async — all `.Result`/`.Wait()` grep hits are the domain `Result` type or properties of awaited values (`RunCalendarAutoSyncCommand.cs:75`, `GetCalendarEventsQuery.cs:89` verified); no lazy-loading proxies configured ⇒ no lazy round-trip risk; no raw entity serialization — every handler projects to DTO records and no controller returns domain entities; named hot reads are set-based: `GetHabitsQuery` (1 query + in-memory tree), `GetDailySummaryQuery` (filtered include + IMemoryCache + time-bucketed key), `GetStreakInfoQuery`/`UserStreakService` (4 set-based queries, `Contains` over ids), `GetGamificationProfileQuery` (2 queries + static catalog join); index inventory otherwise matches hot shapes — Habits `(UserId, IsDeleted)` + partial scheduler indexes (`20260410235000_AddPerformanceIndexes.cs`), HabitLogs `(HabitId, Date)`, StreakFreezes `(UserId, UsedOnDate)`, UserAchievements `(UserId, AchievementId)`; bulk habit commands (delete/log/skip) batch their habit fetches; `GetHabitScheduleQuery` loop is intentional paging.

## Verdict

**20 findings — 1 HIGH · 9 MED · 10 LOW** (web 1 MED / 4 LOW · mobile 4 MED / 4 LOW counting the cross-platform stroke-dashoffset row once under web · API 1 HIGH / 4 MED / 3 LOW).

No findings in: sync-over-async, response over-fetch/raw-entity serialization, `transition-all`, web image conventions, mobile transparent-backdrop JS-thread animation, elevation+overflow:'hidden' (same-file), `useAnimatedStyle` state-per-frame misuse.
