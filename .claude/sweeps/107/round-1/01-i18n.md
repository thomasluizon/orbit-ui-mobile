# Sweep #1 — i18n parity + hardcoded strings (issue #107, round 1)

## Scope swept

Working tree (uncommitted changes included) of:

- `orbit-ui-mobile`: `packages/shared/src/i18n/{en.json,pt-BR.json}` (deep structural diff via node, 1633 leaf keys each); all `.ts/.tsx` under `apps/web/{app,components,hooks,lib,stores}`, `apps/mobile/{app,components,hooks,lib,stores}`, `packages/shared/src` (tests excluded for hardcoded-string findings, included as reference corpus for orphan analysis). Scans: JSX text nodes (same-line + bare multi-line + inline `{'...'}` expressions), attribute literals (`placeholder`, `aria-label`, `alt`, `title`, `label`, `accessibilityLabel`, `accessibilityHint`, `confirmLabel`, etc.), toast/alert/error-state calls (`toast.*`, `Alert.alert`, `showError/showSuccess/showQueued`, `setError`, `setSendError`, `throw new Error`), navigation titles, default props, object-literal `label:/title:/message:` fields, weekday/month arrays, share/clipboard payloads, web metadata, public legal pages, service worker, `app.json`.
- `orbit-api` (branch `chore/107-code-health-sweep`, backend tail only): `Orbit.Api/Controllers/*`, `Orbit.Application/**`, `Orbit.Domain/**` for English display sentences flowing into response payloads (`Result.Failure`, inline `BadRequest/NotFound/Unauthorized` payloads, FluentValidation messages, `ErrorCodes`, `ResultActionResultExtensions`); LocaleHelper surfaces verified exempt.
- Orphan analysis method: every en.json key checked for quoted full-path/suffix/leaf references across the whole corpus, then every candidate manually verified against dynamic constructors (`habits.frequency.every${unit}` / `everyN${unit}s`, `preferences.color${Scheme}`, `upgrade.plans.proFeatures.${key}`, `upgrade.features.${key}.label|free|pro|tooltip`, `upgrade.categories.${cat}`, `profile.facts.${norm}` with `normalizeUserFactCategory`'s closed set, i18next plural suffixes) and against orbit-api (no backend-driven key references). Uncertain candidates were dropped.

## Findings

### Locale key gaps / parity

- MED · packages/shared/src/i18n/en.json + pt-BR.json (key `profile.facts.default` missing from BOTH) · packages/shared/CLAUDE.md §i18n parity + apps/mobile/CLAUDE.md:11 · `apps/mobile/app/ai-settings.tsx:250` calls `` t(`profile.facts.${norm}`) `` and `normalizeUserFactCategory` (packages/shared/src/utils/user-facts.ts:14) returns `'default'` for unknown categories; key is undefined so pt-BR users see raw "DEFAULT". Add `profile.facts.default` to both locales.

### Hardcoded user-facing strings (apps)

- MED · apps/web/app/(app)/ai-settings/page.tsx:92 · apps/web/CLAUDE.md:12 ("All user-facing strings through i18n") · fact category chip renders `category.toUpperCase()` — raw English enum (PREFERENCE/ROUTINE/CONTEXT) — while mobile localizes via `` t(`profile.facts.${norm}`) `` (ai-settings.tsx:250). Use the same t() call; also a cross-platform parity break (root CLAUDE.md §Cross-platform parity).
- LOW · apps/web/app/(app)/calendar-sync/page.tsx:172 + apps/mobile/app/calendar-sync.tsx:502 · apps/web/CLAUDE.md:12 / apps/mobile/CLAUDE.md:11 · "Google Calendar" hardcoded as JSX text while key `calendar.title` exists and is used elsewhere; only "Orbit"/"Astra" are exempt brands. Replace with `t('calendar.title')`.
- LOW · apps/mobile/hooks/use-push-notifications.ts:328,489,495 · apps/mobile/CLAUDE.md:11 · hardcoded English in the hook's exported `error` display state ('Push token is unavailable on this device.', 'Failed to sync push unsubscription.'); currently no consumer renders `error`, but it is public display state. Store i18n keys (or t() at the boundary).
- LOW · apps/web/hooks/use-push-notification-preferences.ts:146 · apps/web/CLAUDE.md:12 · `throw new Error('Failed to subscribe to push notifications')` — mutation error message authored as display text. Use an i18n key.
- LOW · apps/web/components/goals/goal-list.tsx:169 · apps/web/CLAUDE.md:12 · `aria-roledescription="draggable item"` is announced by screen readers in English regardless of locale. Route through t().
- LOW · apps/mobile/hooks/use-push-notifications.ts:178 · apps/mobile/CLAUDE.md:11 · Android notification channel `name: 'Default'` is shown in system notification settings; not localized. Localize at channel creation (`i18n.t`).

### Orphan locale keys (defined in BOTH locales, zero static or plausible dynamic reference; en.json line cited, pt-BR mirrors)

- LOW · packages/shared/src/i18n/en.json:110,130,146 · root CLAUDE.md Code Standards rule 2 (delete unused code) · `common.noResults`, `common.yearlyBadge`, `common.moreActions` unused. Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:72 · rule 2 · `notifications.deleteQueuedMultiple` unused (only `deleteQueued`/`deleteUndo` referenced; no plural-suffix or dynamic builder). Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:451,453 · rule 2 · `habits.form.templateName`, `habits.form.deleteTemplate` unused (only `templateNamePlaceholder` / `deleteTemplateError` referenced). Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:494 · rule 2 · `habits.detail.streakDays` ("{n}d") unused. Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:754,758,759,770,772 · rule 2 · `profile.facts.add`, `.factPlaceholder`, `.category`, `.confirmBulkDelete`, `.limitReached` unused; dynamic `` profile.facts.${norm} `` only reaches `preference|routine|context|default`. Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:996 · rule 2 · `trial.expired.subtitle` unused (UI uses `subtitleQuiet`; `TRIAL_EXPIRED_FEATURE_KEYS` does not include it). Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:1015 · rule 2 · `metrics.longestStreak` unused (no `metrics.` namespace usage; `habits.detail.longestStreak` / `streakDisplay.detail.longestStreak` are separate, used keys). Delete from both locales (check siblings `metrics.currentStreak`/`metrics.monthly` before removing the block — they matched quoted-leaf references and were NOT verified orphans).
- LOW · packages/shared/src/i18n/en.json:1306 · rule 2 · `upgrade.currentPlan` unused (leaf appears nowhere in either app). Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:1401-1409 · rule 2 · entire `upgrade.plans.free.*` block unused (`name`, `cta`, `features.habits`, `features.ai`, `features.theme`, `features.ads`); upgrade pages render the Free column via `upgrade.free` + `upgrade.features.${key}.free`, and no `plans.free` reference or dynamic constructor exists. Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:1420 · rule 2 · `upgrade.plans.yearly.recommended` unused (`yearly.name/period/cta/includesMonthly` are used; no dynamic path reaches `recommended`). Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:1434 · rule 2 · `upgrade.plans.proFeatures.allFeatures` unused — `` upgrade.plans.proFeatures.${feature.key} `` draws from the closed sets `UPGRADE_PRO_FEATURES`/`UPGRADE_YEARLY_EXTRA_FEATURES` (packages/shared/src/utils/upgrade.ts:72-86) which never contain `allFeatures`. Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:1561 · rule 2 · `goals.streak.daysRemaining` unused (only `goals.streak.ofTarget` referenced). Delete from both locales.
- LOW · packages/shared/src/i18n/en.json:1855 · rule 2 · `orbitMcp.keyCreated` unused (only `keyCreatedWarning` referenced). Delete from both locales.

### Backend tail (orbit-api English display sentences in response payloads)

- HIGH · orbit-api src/Orbit.Application + src/Orbit.Domain (101 sites) · issue #107 agent-1 (English display sentences in responses must be client-localized codes) · 101 code-less `Result.Failure("English sentence")` flow into `{ error }` payloads — e.g. LogHabitCommand.cs:90,93,100; SkipHabitCommand.cs:41,77,80,86,98; MoveHabitParentCommand.cs:37,47; CreateTagCommand.cs:24; UpdateTagCommand.cs:33; CreateUserFactCommand.cs:32; GetCalendarMonthQuery.cs:40; GetDailySummaryQuery.cs:39; GetRetrospectiveQuery.cs:47; SubscribePushCommand.cs:27,32,44; subscription commands/queries; Domain entity guards (ApiKey.cs:29-48, ChecklistTemplate.cs:18-48, Goal.cs:56+). ~47 frontend call sites display these verbatim via `getErrorMessage` (e.g. apps/web/app/(app)/calendar-sync/page.tsx:123, apps/mobile/app/(tabs)/profile.tsx:345) → pt-BR users see English errors. Fix: assign `ErrorCodes` and have clients map code→i18n key (machinery already exists: `extractBackendErrorCode` + `getFriendlyErrorKey` code branch).
- HIGH · orbit-api src/Orbit.Api/Controllers/AuthController.cs:48,97,158,224,277,325,347 · issue #107 agent-1 · auth endpoints serialize `new { error = result.Error }` and DROP `result.ErrorCode` even where codes exist (`TOO_MANY_ATTEMPTS`, `CODE_EXPIRED`, `INVALID_VERIFICATION_CODE` set in VerifyCodeCommand.cs:60,65,73); two sentences have no code at all (SendCodeCommand.cs:49 + RequestAccountDeletionCommand.cs:30 "Please wait before requesting a new code", ConfirmAccountDeletionCommand.cs:28, GoogleAuthCommand.cs:69,77). This forces the client to exact-match English copy (`AUTH_BACKEND_ERROR_MAP`, packages/shared/src/utils/auth-login.ts:3-9) — any backend wording change silently degrades localization to generic keys. Fix: include `errorCode` in auth error payloads + add missing codes; key the client off codes.
- MED · orbit-api src/Orbit.Api/Controllers (HabitsController.cs:164-542 ~12 sites; GoalsController.cs:53,151,165; ChecklistTemplatesController.cs:27,46,63; AiController.cs:221,258; ApiKeysController.cs:76; AuthController above) · root CLAUDE.md rule 1 (root cause over workarounds) + #107 · controllers hand-roll `new { error = result.Error }` although `ResultActionResultExtensions` (src/Orbit.Api/Extensions/ResultActionResultExtensions.cs:14-43) already emits `errorCode` — this bypass is why codes never reach the wire. Fix: route failure serialization through the shared extension.
- MED · orbit-api src/Orbit.Application (64 `.WithMessage(...)` sites in validators) · issue #107 agent-1 · FluentValidation English sentences flow to clients as field errors; the client localizes them by SUBSTRING-matching English (`CONTEXTUAL_RULES`, packages/shared/src/utils/error-utils.ts:205-250) and unmatched messages render raw English (`extractBackendError` prioritizes the first field error). Fix: stable error codes / validation keys instead of prose.
- LOW · orbit-api src/Orbit.Api/Controllers inline English payloads · issue #107 agent-1 · AiController.cs:181,288 ("Pending operation not found or expired."); CalendarController.cs:75; ChatController.cs:40,85,196,202,205,216,228,241,248; SyncController.cs:329,332; OAuthController.cs:189,197,202. Replace with codes (ErrorCodes already covers several, e.g. `MESSAGE_TOO_LONG`, `CHAT_HISTORY_TOO_LARGE`, `INVALID_GOOGLE_TOKEN`). Note: OAuthController.cs:253 `error`/`error_description` follows the OAuth 2.0 wire format (machine code present) — not counted.

## Verified NOT findings (do not re-flag)

- en/pt-BR key parity: PERFECT — 1633 keys each, zero missing either direction, no structural mismatches. The 9 identical en==pt values are brand/format strings (Google Calendar, Orbit Pro, Claude Web/Desktop/Code, `{current} / {next} XP`, `{previous} -> {value} {unit}`).
- Dynamic key families all resolve: `habits.frequency.every*` (habit-card-helpers.ts:105-108), `preferences.color*` (preferences pages + onboarding), `upgrade.features.*` / `upgrade.categories.*` / `upgrade.plans.proFeatures.*` (upgrade pages), `profile.facts.{preference,routine,context}`.
- `alt="Orbit"` (app-logo), "Astra" text node (today-ai-summary) — exempt brands. CHAT_SPEECH_LANGUAGES labels are endonyms (locale-invariant data). `days: ['Monday',...]` in tour-mock-data are API data values localized at render. Composed accessibility labels (`${title}. ${description}`, `${checkedCount}/${items.length}`) are i18n'd parts/data. google-auth.ts thrown messages never reach UI (resolveAuthLoginErrorKey maps to keys). test-mocks/date-fns month array is test infra.
- Web public pages (/terms, /privacy, /delete-account), not-found.tsx, toasts, Share/clipboard payloads, Zod validation messages, navigation titles: fully i18n'd — zero hardcoded display text found.
- orbit-api LocaleHelper surfaces (emails via ResendEmailService, push via ReminderSchedulerService/GoalDeadlineNotificationService/StreakFreezeAutoActivationService/AiSlipAlertMessageService, AI via AiSummaryService/AiRetrospectiveService/AiGoalReviewService, gamification achievement names PT/EN table) are server-localized — exempt per sweep brief. Webhook/RTDN handler messages (HandleWebhookCommand, HandlePlayNotificationCommand) respond to Stripe/Google, not users.

## Verdict

2 HIGH · 4 MED · 19 LOW (25 finding lines; orphan lines cover 25 dead keys)
