# Sweep #4 — Accessibility, ROUND 6 VERIFY (read-only final) — issue #107

Baseline: ui-mobile `1dd5c3d` (committed green; round-6 a11y commit, working tree clean). Static analysis only, both platforms. Contrast computed via `.claude/sweeps/107/round-5/amber-bad-surfaces.mjs` (re-run, hex-only first-principles WCAG) + ad-hoc tint/ring blends. Scope: (1) verify every round-6 F1 (17-site amber-as-text) + F3 (today-shell focus, mobile skeleton-pulse + CheckCircle-pop reduced-motion) fix landed; (2) FRESH exhaustive scan for any remaining status-base-as-text / fg-4-as-text / unnamed-interactive / outline-none-without-replacement / non-monotonic-heading / ungated-reduced-motion, EXCLUDING the 5 registered deferrals (DEF-R4-A11Y-1/2/3, DEF-R6-A11Y-1, DEF-R6-A11Y-2).

---

## PART A — round-6 F1/F3 fixes: ALL LANDED ✓

### Tokens (source-verified)
- web `globals.css`: light `--status-overdue-text:#b45b00` / `--status-bad-text:#e7000b`; dark both `#fe9a00` / `#fb2c36` (`:194-198,239-243`). `--color-*` aliases wired `:36-40`.
- mobile via shared `neutral-ramp.ts:216-226`: light `overdueText:#b45b00` / `badText:#e7000b` / `fgOnBad:#ffffff`; dark `#fe9a00` / `#fb2c36` / `#020618`. Surfaced `theme.ts:124-130,205-209,242-246` as `statusOverdueText`/`statusBadText`.
- Recomputed: overdue-text light **4.52** canvas / **4.73** white-card / 9.44 dark ✓. bad-text(=base) light **4.56** canvas / **4.77** white-card / 5.29 dark ✓.

### F1 — 17 amber-as-text sites → `-text` token (all migrated)
- web delete-account-modal.tsx:279,346 → `var(--status-overdue-text)` ✓
- web fresh-start-modal.tsx:286 ✓ · login-sections.tsx:162 ✓ · goal-detail-drawer.tsx:548 ✓ · referral-drawer.tsx:77 ✓ · create-api-key-modal.tsx:386 ✓ · push-prompt.tsx:177 ✓ · advanced/page.tsx:327 ✓
- web goal-card.tsx: split into `progress.color` (fill, base) + `progress.textColor` (`-text`); deadline `:64` (≤7d) → `-text`, overdue `:58` → `--status-bad` (4.77 white-card ✓); `%` consumes `textColor` `:193`; ProgressBar consumes `color` `:208` ✓
- mobile advanced.tsx:378 (`messageText`) · goal-detail-drawer/styles.ts:84 (`warningText`) · referral-drawer.tsx:217 (`errorText`) · create-api-key-modal.tsx:516 (`warningText`) · push-prompt.tsx:241 (`retryText`) · login.tsx:684 (`inlineError`) → `tokens.statusOverdueText` ✓
- mobile goal-card.tsx (`components/goal-card.tsx`, not `components/goals/`): same split — `progressColor` (fill) `:59` + `progressTextColor` (`-text`) `:75`; `%` consumes `progressTextColor` `:212`, ProgressBar consumes `progressColor` `:225`; deadline overdue→`statusBad` `:99` / ≤7d→`statusOverdueText` `:105`, on `bgCard` (4.77 ✓) ✓

### F3 — focus + reduced-motion (all landed)
- today-shell.tsx: input keeps `outline:none` `:275` but the **wrapper** `:258` now paints `focus-within:shadow-[inset_0_0_0_2px_var(--primary)]` (resting `inset 1px --hairline`). Computed ring `--primary` vs `--bg-elev` across 6 schemes × 2 modes: **min 3.22** (green light), all ≥3:1 ✓ (1.4.11 satisfied; replaces the old ~1.4:1 hairline alpha shift).
- mobile skeleton.tsx:12-19 — `usePulseOpacity()` calls `usePrefersReducedMotion()`; on reduce it `setValue(1)` + early-returns before `Animated.loop` ✓.
- mobile habit-row.tsx:603-615 — CheckCircle pop reads `usePrefersReducedMotion()`; `withSequence(withSpring…)` gated by `!prefersReducedMotion` in the `:608` condition → parities web `animate-check-pop` killed by the global media query ✓.

---

## PART B — registered deferrals: verified intact, NOT reported
- **DEF-R6-A11Y-1** pastDue badge — web upgrade/page.tsx:417-418 + mobile upgrade.tsx:654-657 still base `--status-bad`/`tokens.statusBad` on bad@18% tint (correctly NOT no-op-migrated; light 3.30 ceiling). Accepted.
- **DEF-R4-A11Y-3** amber badge/expiry-warning + **trial-banner** (web:47,75,90 / mobile:92,130) + the in-page trial block (web upgrade:708 / mobile upgrade:754, on overdue@10% tint `:691`/`:737`) — amber-on-amber@10% tint, `-text`→4.06 ceiling. Same-class as the registered expiry-warning; excluded.
- **DEF-R4-A11Y-1** (`--primary` accent-text), **DEF-R4-A11Y-2** (goal-chip primary-soft) — not re-reported.
- **DEF-R6-A11Y-2** (web habit-row APG nested-button) — not re-reported.

---

## PART C — FRESH EXHAUSTIVE SCAN

### Clean categories (no findings)
- **fg-4-as-text:** web — every `--fg-4` hit is a decorative/redundant icon or chevron (sizes 16-36, several `aria-hidden`), non-text 3:1-floor, NOT text. mobile — zero `color: tokens.fg4`; all `fg4` is `borderColor`/`textDecorationColor`/SVG `stroke`/tooltip-arrow bg. Category fully closed (matches round-5).
- **bad-as-text on SOLID surfaces:** web (~20 sites) + mobile (~24 sites) error/destructive copy use base `--status-bad`/`tokens.statusBad` on plain modal/card/sheet — bad-as-text passes AA on every solid surface (4.56 canvas / 4.77 white-card / 5.29 dark) per the corrected calculator. Not findings.
- **outline-none-without-replacement (web):** all 16 `outline:none`/`outline-none` sites carry a focus replacement — same-line `focus:shadow-[inset_0_0_0_2px_var(--primary)]` (support-field, modals, form-fields, app-select/date-picker, create-api-key), a `focus-within` wrapper ring (today-shell:258, create-habit-modal:287, field-input:63, chat-composer-bar:371), or a JS `activeIndex`-driven `inset 2px --primary` ring (code-input:76-79). None bare.
- **reduced-motion:** mobile — all 5 production looping files gate on `usePrefersReducedMotion` (skeleton, habit-row CheckCircle, typing-indicator:15, level-up-overlay:60, chat-animations, celebration-motion). web — global `@media (prefers-reduced-motion: reduce){ *{ animation-iteration-count:1!important } }` (globals.css:900-905) kills every infinite animation. Clean.
- **heading hierarchy:** web has only `<h2>`/`<h3>` (no h1/h4/h5/h6). The one round-3-flagged file (advanced/page.tsx) is monotonic h2(:118)→h3(:568,599). Remaining `<h3>` are local card/drawer/tooltip titles (pre-existing pattern, never flagged). No new skip evident.

### MED — bad-text on bad@10% tint (tinted status callouts, NOT the registered pastDue badge)
Same unfixable ceiling SHAPE as DEF-R6-A11Y-1 (bad text on bad tint, can't reach 4.5 without a DESIGN.md tint/token redesign — `-text` is a light no-op for bad), but at sites the register does NOT enumerate (DEF-R6-A11Y-1 is scoped to "the hand-rolled pastDue billing badge" only). Surfaced here for an explicit ceiling-vs-fix decision rather than swallowed. Computed bad `#e7000b` on bad@10% over canvas = **3.82 light ✗** / 4.99 dark ✓; none AA-large-exempt.

- MED · apps/web/components/chat/conflict-warning.tsx:18 · bad-on-bad-tint · HIGH-severity callout `bg-[var(--status-bad)]/10 text-[var(--status-bad)]`, text-xs(12px) regular (`:46`) → darken tint/fill or register as a DESIGN.md ceiling alongside DEF-R6-A11Y-1.
- MED · apps/mobile/components/chat/conflict-warning.tsx:22-23 · bad-on-bad-tint · HIGH callout `statusBadText` on `statusBad`@1A, 12px (parity of web:18) · same fix.
- MED · apps/web/components/chat/action-chips.tsx:78-79 · bad-on-bad-tint · "Failed" chip `color:var(--status-bad)` on `var(--status-bad)`@10%, 13px/500 (`:134-135`) · same fix. (Icon `:156` non-text passes 3:1; sibling error `:167` is on solid → passes.)
- MED · apps/mobile/components/chat/action-chips.tsx:182-183 · bad-on-bad-tint · "Failed" chip `statusBadText` on `statusBad`@1A, 13px/500 (`:213-215`) (parity) · same fix. (`errorText:217-220` is a sibling on solid → passes.)

### MED — amber-base-as-text on a SOLID card (un-propagated F1 fix; directly fixable, NOT a ceiling)
The billing usage value renders amber on the card surface (NOT a tint) when usage > 80%, so it is the exact F1 class round-6 fixed everywhere else but missed here. Base `--status-overdue` on white card = **3.20 light ✗**; the existing `-text` token → 4.73 ✓ (mode-aware, keeps `#fe9a00` dark). 13px, not AA-large-exempt.

- MED · apps/web/app/(app)/upgrade/page.tsx:95 · status-overdue-as-text · `UsageStats` value `color: usageUrgent ? 'var(--status-overdue)'` on `cardSurface` → `var(--status-overdue-text)`. (`:107` ProgressBar `color` is a fill, leave.)
- MED · apps/mobile/app/upgrade.tsx:147 · status-overdue-as-text · `usageValue` `color: usageUrgent ? tokens.statusOverdue` on card (parity) → `tokens.statusOverdueText`. (`:159` ProgressBar `color` is a fill, leave.)

### LOW — mobile interactive elements missing `accessibilityRole` (4.1.2; accessible NAME present)
`move-parent-dialog.tsx` is a dialog the round-3/4 D9 role sweep did not reach (it fixed confirm-dialog/tour-tooltip/checklist/app-time-picker, not this one). Each control composes an accessible name from its `<Text>` children but lacks `accessibilityRole="button"`; screen readers announce the name but not the role.

- LOW · apps/mobile/components/habit-list/move-parent-dialog.tsx:61 · unnamed-interactive · backdrop TouchableOpacity (`onPress={onClose}`) has NO name AND no role and is NOT hidden — unlike the round-3-fixed confirm-dialog backdrop (role + `common.close`) and anchored-menu backdrop (`accessibilityElementsHidden`). · Hide it (`importantForAccessibility="no"`) or label it.
- LOW · apps/mobile/components/habit-list/move-parent-dialog.tsx:101,136,149 · missing-role · option/cancel/confirm Pressables (names via Text children) lack `accessibilityRole="button"`.
- LOW · apps/mobile/components/habit-list/drill-panel.tsx:68 · missing-role · "add sub-habit" TouchableOpacity (name via Text `:74`) lacks role.
- LOW · apps/mobile/components/habits/habit-form-fields/slip-alert-section.tsx:49 · missing-role · upgrade-CTA TouchableOpacity (name via Text `:60`) lacks role.
- LOW · apps/mobile/components/habits/goal-linking-field.tsx:59 · missing-role · goal-toggle chip (name via Text `:70`) lacks `accessibilityRole="button"` + `accessibilityState={{selected}}` (selection is style-only: ring/fill/bold).
- LOW · apps/mobile/components/gamification/welcome-back-toast.tsx:168 · missing-role · dismiss Pressable has `accessibilityLabel` `:177` but no role.

(anchored-menu.tsx:136 backdrop is correctly `importantForAccessibility="no"` + `accessibilityElementsHidden` — not a finding.)

---

## Verdict

**NOT clean.** All round-6 F1 (17 amber-as-text) and F3 (today-shell focus ≥3:1, mobile skeleton + CheckCircle reduced-motion) fixes LANDED and verified. The 5 registered deferrals are intact and correctly not no-op-migrated. The fresh exhaustive scan surfaces a tinted-status-callout family the prior rounds never enumerated:

| Finding | Sev | Sites |
|---|---|---|
| bad-text on bad@10% tint — conflict-warning HIGH + action-chips Failed chip (unregistered ceiling twins of DEF-R6-A11Y-1) | MED | 4 (web 2 + mobile 2) |
| amber-base-as-text on solid card — billing usage value (un-propagated F1 fix; directly fixable) | MED | 2 (web + mobile) |
| mobile interactive missing accessibilityRole — move-parent-dialog (4 incl. unnamed backdrop) + drill-panel + slip-alert + goal-linking chip + welcome-back toast | LOW | 9 |

**STILL-OPEN: 15 findings — 0 HIGH · 6 MED · 9 LOW** (excluding the 5 registered deferrals).

Notes: the 6 MED split into one genuinely-fixable propagation miss (billing usage value, fix = existing `-text` token, 3.20→4.73) and four tinted-callout ceilings (3.82, same DESIGN.md tint-redesign shape as the accepted DEF-R6-A11Y-1 pastDue badge — flagged for a ceiling-vs-fix decision, not silently swallowed, since the register names only the pastDue badge). The 9 LOW are 4.1.2 role-only gaps on a dialog/components the D9 role sweep didn't reach (accessible NAME already present in every case; one redundant backdrop has neither). conflict-warning's MEDIUM-severity branch (amber-on-amber tint) is excluded as DEF-R4-A11Y-3 class. No status-bad-on-solid, fg-4-as-text, bare outline-none, ungated reduced-motion, or new heading-skip remain.
