# Round-6 verify — Sweep #1 i18n key parity — issue #107

READ-ONLY. Baseline: ui-mobile **1dd5c3d** (committed green). Ran an inline en/pt-BR leaf-diff (`node`) over `packages/shared/src/i18n/{en,pt-BR}.json`; confirmed r6 introduced no new missing/orphan keys.

## Key parity en ↔ pt-BR — PERFECT, 1628 == 1628

Inline leaf-diff (recursive leaf flatten of both locale objects):

```
en leaves: 1628
pt leaves: 1628
in en, missing in pt: []
in pt, missing in en: []
```

Exactly the 1628 the brief specified, zero drift either direction. r6 added no i18n keys (the locale files are absent from `git diff --name-only 3520d10 1dd5c3d`), so the round-5 count carries forward unchanged.

## No new missing / orphan from round 6

- r6 touched only `.tsx`/`.ts`/`.css` source + test files + the eslint/next config + sweep docs — neither `en.json` nor `pt-BR.json` is in the diff. No new `t()` reference keys were introduced (the amber→-text migration, reduced-motion gating, and config-comment edits add no user-facing copy).
- The web focus-ring + `-text` token swaps and mobile reduced-motion gating are styling-only — no new translatable strings, no removed keys.

The round-5 referenced-but-missing (zero) and orphan (11 dynamic-builder non-findings) baselines are undisturbed.

## Verdict

**ZERO FINDINGS.** en/pt-BR key parity is exact at 1628 == 1628 (0 missing either direction). Round 6 added/removed no i18n keys (locale files not in the r6 diff), so no new missing or orphan keys were introduced. Net: round-5 ZERO → round-6 ZERO.
