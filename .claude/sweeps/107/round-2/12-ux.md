# Sweep #12 ROUND 2 — UX states + copy (issue #107)

Read-only re-audit at baseline `ae5c150` (recovery commit). Wave-2 fix agents died mid-edit; recovery-notes.md states partition completeness is unverified and "unapplied fixes resurface as findings." This sweep is that definitive check.

Method: every round-1 finding (F1–F14) re-verified against current code with file:line evidence. Plus a fresh domain sweep (raw `error.message` renders, em dashes / SaaS filler, offline-state copy quality). Both platform files named per finding.

## Round-1 verification matrix

| R1 | Domain | Verdict now | Evidence |
|---|---|---|---|
| F1 | web drill error unrendered | **STILL OPEN** | web `drill-content.tsx` has no `drillError` prop/branch; mobile `habit-list.tsx:1634` renders `drill.drillError` |
| F2 | mobile profile raw error.message | **STILL OPEN** | mobile `profile.tsx:542` `error instanceof Error ? error.message : …` (no dev gate); web `profile/page.tsx:196` dev-gated |
| F3 | mobile create-key raw err.message | **STILL OPEN** | mobile `advanced.tsx:170` `err instanceof Error ? err.message : …`; web `advanced/page.tsx:215` uses `t('orbitMcp.createKeyError')` |
| F4 | web fetchApiKeys swallows !ok | **STILL OPEN** | web `advanced/page.tsx:48` `if (!res.ok) return []` |
| F5 | user-facts load error masked as empty | **STILL OPEN** | web `ai-settings/page.tsx:38` `return []`; mobile `ai-settings.tsx:375-406` has no `factsQuery.error` branch |
| F6 | web fact category pill untranslated | **STILL OPEN** | web `ai-settings/page.tsx:92` `category.toUpperCase()`; mobile `ai-settings.tsx:248` translates via `t(profile.facts.${norm})` |
| F7 | mobile no ErrorBoundary + web (chat) error.tsx | **STILL OPEN** | grep `ErrorBoundary` across `apps/mobile/app` = 0 hits; `apps/web/app/(chat)/error.tsx` absent (only (app)+(auth) have one) |
| F8 | mobile calendar-sync thin empty/loading | **STILL OPEN** | mobile empty `calendar-sync.tsx:637-644` bare text (no glyph/action); loading `:577-587` bare text (no spinner); web has CalendarDays glyph + Loader2 spinner |
| F9 | mobile widget features list missing | **STILL OPEN** | mobile `advanced.tsx:452-458` ConfirmDialog = 3 concatenated steps only; web `advanced/page.tsx:627-656` renders `WIDGET_FEATURES` (4 icon items) |
| F10 | mobile delete-account offline borrowed copy | **STILL OPEN** (now superseded by F15) | mobile `profile.tsx:933-934` title `calendarSync.notConnected`, desc = `sendCode / confirmDelete` |
| F11 | web achievements lock glyph missing | **STILL OPEN** | web `achievements/page.tsx:52-92` text-only; mobile `achievements.tsx:81-89` Lock glyph circle |
| F12 | mobile data-export getErrorMessage leak | **STILL OPEN** | mobile `profile.tsx:345` `getErrorMessage(err, t('dataExport.error'))`; web `profile/page.tsx:134` fixed `t('dataExport.error')` |
| F13 | mobile create-key no capabilities gate | **STILL OPEN** | mobile `advanced.tsx:101` `canCreateKey = apiKeys.length < MAX`; no `!capabilitiesQuery.error`/`scopeOptions.length>0`; chip `:316` always enabled; no capabilities-error display. web `advanced/page.tsx:174-178` `canCreateScopedKey` |
| F14 | revoke confirm surface divergent | **STILL OPEN** | web `advanced/page.tsx:441-482` inline status-bad banner; mobile `advanced.tsx:460-472` kit ConfirmDialog |

**14/14 round-1 findings STILL OPEN.** Zero round-1 UX fixes survived the recovery. (Consistent with recovery-notes: only build/test/lint repair was done; no sweep-12 partition was applied.)

## Findings (round 2)

### Still-open from round 1 (carried forward — same fix as round 1)

- **F1 · MED** · web `apps/web/components/habits/habit-list/drill-content.tsx` (no `drillError` branch) + mobile `apps/mobile/components/habit-list.tsx:1634` · check 3 error-state · A failed sub-habit fetch renders the "No sub-habits yet + Add" empty prompt on web (empty-as-error); mobile shows the error text. The web hook `hooks/use-drill-navigation.ts` still sets `drillError` but no web component consumes it. · Fix: render `drill.drillError` in the web drill panel like mobile.

- **F2 · MED** · web `apps/web/app/(app)/profile/page.tsx:196` + mobile `apps/mobile/app/(tabs)/profile.tsx:542` · check 3 · Mobile renders raw `error.message` in production (no `NODE_ENV` gate); web gates it dev-only and shows `t('errors.loadProfile')` in prod. · Fix: mirror web's dev-only gate on mobile.

- **F3 · MED** · web `apps/web/app/(app)/advanced/page.tsx:215` + mobile `apps/mobile/app/advanced.tsx:170` · check 3 · Mobile create-API-key failure surfaces raw `err.message`; web uses fixed `t('orbitMcp.createKeyError')`. Different fallback keys for the same op (`apiKeys.errors.create` vs `orbitMcp.createKeyError`). · Fix: mobile uses the same friendly key (or `getErrorMessage`) and aligns the fallback key.

- **F4 · MED** · web `apps/web/app/(app)/advanced/page.tsx:48` + mobile `apps/mobile/app/advanced.tsx:235` (designed error branch, for contrast) · check 3 · web `fetchApiKeys` returns `[]` on `!res.ok`, so a 500 renders "No API keys yet" and the `apiKeysQuery.error` branch (web ~338) is unreachable; mobile's `apiClient` throws and shows `orbitMcp.apiKeysError`. · Fix: throw on `!res.ok` in the web fetcher.

- **F5 · MED** · web `apps/web/app/(app)/ai-settings/page.tsx:38` + mobile `apps/mobile/app/ai-settings.tsx:375-406` · check 3 · user-facts load failure masked as the "no facts yet — Ask Astra" empty state on BOTH (web fetcher returns `[]` on `!ok`; mobile chain has no `factsQuery.error` branch). · Fix: add an error branch (+ retry) on both; web fetcher throws on `!ok`.

- **F6 · MED** · web `apps/web/app/(app)/ai-settings/page.tsx:92` + mobile `apps/mobile/app/ai-settings.tsx:248` · check 5 copy · Web renders the raw English category slug uppercased (`category.toUpperCase()` → "PREFERENCE" in pt-BR); mobile translates via `t('profile.facts.'+norm)` ("PREFERÊNCIA"). Keys exist in both locales. · Fix: web uses the `profile.facts.*` keys.

- **F7 · MED** · web `apps/web/app/(app)/error.tsx` + `apps/web/app/(auth)/error.tsx` present, `apps/web/app/(chat)/error.tsx` MISSING + mobile `apps/mobile/app/_layout.tsx` exports no `ErrorBoundary` (D28 directed both; NEITHER landed) · check 3 · Mobile has no designed render-error fallback anywhere; web (chat) segment lacks the route-error boundary its siblings have. · Fix: export an `ErrorBoundary` from `apps/mobile/app/_layout.tsx` mirroring web `error.tsx`; add `apps/web/app/(chat)/error.tsx`. (Verified against triage D28: required artifacts do NOT exist.)

- **F8 · MED** · web `apps/web/app/(app)/calendar-sync/page.tsx:412-439,458-471` + mobile `apps/mobile/app/calendar-sync.tsx:637-644,577-587` · checks 1-2 · Mobile empty = bare text (no CalendarDays glyph, no go-back action); mobile loading = bare text (no spinner). Web has both. · Fix: add glyph + action to the mobile empty block and a spinner to the loading block.

- **F9 · MED** · web `apps/web/app/(app)/advanced/page.tsx:627-656` + mobile `apps/mobile/app/advanced.tsx:452-458` · check 5 · Mobile widget info = 3 concatenated steps in a plain ConfirmDialog; the shared `WIDGET_FEATURES` list (+ `profile.widgetHow.feature1-4`) is unreachable on the platform that has the widget. · Fix: render `WIDGET_FEATURES` on mobile.

- **F11 · LOW** · web `apps/web/app/(app)/achievements/page.tsx:52-92` + mobile `apps/mobile/app/achievements.tsx:81-89` · check 1 · Mobile locked block has a Lock glyph circle; web locked block is text-only + CTA. · Fix: add the Lock glyph on web.

- **F12 · LOW** · web `apps/web/app/(app)/profile/page.tsx:134` + mobile `apps/mobile/app/(tabs)/profile.tsx:345` · check 3 · Data-export failure: web fixed `t('dataExport.error')`; mobile `getErrorMessage(err, …)` can surface backend/raw strings mobile-only. · Fix: align mobile on the fixed friendly key.

- **F13 · LOW** · web `apps/web/app/(app)/advanced/page.tsx:174-178` + mobile `apps/mobile/app/advanced.tsx:101,316` · check 5 · Web disables "Create key" until capabilities load (`canCreateScopedKey`) and shows a capabilities error; mobile keeps the chip enabled on plain `canCreateKey` with no `capabilitiesQuery.error` display — failed capabilities load opens the modal with an empty scope list. · Fix: mirror web's gating + error copy on mobile.

- **F14 · LOW** · web `apps/web/app/(app)/advanced/page.tsx:441-482` + mobile `apps/mobile/app/advanced.tsx:460-472` · check 5 · API-key revoke confirms inline (status-bad banner + chips) on web vs kit ConfirmDialog on mobile. · Fix: pick one (kit ConfirmDialog) for both.

### New this round

- **F15 · MED** · web n/a (no offline mode — BFF online-first, confirmed: zero `isOnline`/`offline`/`WifiOff` in `apps/web/components/chat` + `apps/web/app/(chat)`) + mobile, SYSTEMIC across 7 surfaces · check 3/5 copy quality · Every mobile `OfflineUnavailableState` borrows `t('calendarSync.notConnected')` ("Not Connected" — a calendar-connection status label) as its offline TITLE and concatenates action-button labels as its DESCRIPTION. This is the F10 anti-pattern, but F10 named only 1 of 7 sites. All occurrences:
  - `apps/mobile/app/(tabs)/profile.tsx:933-934` (delete-account — = old F10) — desc `deleteAccount.sendCode / deleteAccount.confirmDelete`
  - `apps/mobile/app/chat.tsx:53-54` (→ `chat-input-area.tsx:120,167`) — desc `chat.send / chat.attachImage / chat.toggleMic`
  - `apps/mobile/app/login.tsx:168-169,469-470` — desc `auth.sendCode / auth.verify / auth.signInWithGoogle`
  - `apps/mobile/app/support.tsx:243-244` — desc `profile.support.send / profile.support.description`
  - `apps/mobile/app/retrospective.tsx:310-311,328-329` (two blocks) — desc `retrospective.generate / retrospective.changePlan`
  - `apps/mobile/app/upgrade.tsx:591-592,903-904` (two blocks) — desc `upgrade.billing.actions.manage / …` and `… / plans.monthly.cta / plans.yearly.cta`
  - Same borrowed title also drives `setError(t('calendarSync.notConnected'))` in inline error setters: `profile.tsx:329,377,397`, `upgrade.tsx:409`, `support.tsx:173`, `retrospective.tsx:193,212`, `advanced.tsx:158`, `calendar-sync.tsx:289,306`.

  A generic offline string already exists in the locales (`en.json:220` `"offline": "You appear to be offline. Check your connection and try again."`), and the round-1 fix author even pre-staged `chat.offline.title`/`chat.offline.description` in `.claude/sweeps/107/i18n-additions/chat.json` — but it was never merged into `en.json`/`pt-BR.json` (grep confirms absent) or wired. · Fix: add dedicated offline title/description keys (one shared pair, or per-surface) in BOTH locales and replace every `calendarSync.notConnected` offline usage; merge the staged `chat.json`. NOTE: a test `apps/mobile/__tests__/screens/intentional-offline-ux.test.tsx:467,476` asserts `texts.toContain('calendarSync.notConnected')` — it codifies the smell and must be updated alongside the copy fix.

- **F16 · MED** · web `apps/web/components/chat/breakdown-suggestion.tsx:157-159` + mobile `apps/mobile/components/chat/breakdown-suggestion.tsx:173-176` · check 3 · The "break this into sub-habits" bulk-create failure stores `err instanceof Error ? err.message : t('errors.bulkCreateHabits')` into `createError`, rendered raw in JSX (web ~line 293 `{createError && <p …>{createError}</p>}`). Because a real backend/network failure IS an `Error`, the friendly fallback never fires and raw strings (e.g. `ECONNREFUSED`, a 500 body) show to the user — on BOTH platforms. The `instanceof Error` ternary is inverted: it shows the friendly copy only for non-Errors. · Fix: always show the friendly i18n copy (or `getErrorMessage(err, t('errors.bulkCreateHabits'))`) on both platforms.

### Checks with ZERO new findings

- **Check 4 (i18n microcopy quality)**: en.json + pt-BR.json — **zero em dashes** (U+2014), **zero banned SaaS filler** (Elevate/Seamless/Unleash/Supercharge/Next-Gen/Effortless/Game-Changing/Revolutionize/Empower/Streamline), key-synced (44/44 top-level via jq on both). Partial wave-2 edits introduced none. CLEAN.
- **Check 6 (navigation consistency)**: unchanged since round 1 (shared `PROFILE_NAV_ITEMS`, identical FAB rules, byte-equivalent confirm cluster). No new findings.
- **Check 7 (first-run/activation)**: unchanged (symmetric onboarding gating + flag writes). No new findings.

## Verdict

**16 findings: 0 HIGH · 9 MED · 5 LOW + 2 NEW MED (F15, F16).**

- All 14 round-1 findings (F1–F14) re-verified **STILL OPEN** — zero sweep-12 fixes survived the wave-2 recovery (recovery-notes confirms only build/test/lint repair ran).
- 2 new MED this round: F15 (systemic borrowed offline copy across 7 mobile surfaces — F10 was 1/7), F16 (inverted `instanceof Error` exposes raw bulk-create errors on both platforms).
- Check 4 / 6 / 7: ZERO FINDINGS.

Per-check: empty (F8, F11) · loading (F8) · error (F1,F2,F3,F4,F5,F7,F12,F15,F16) · copy/i18n (F6, F15) · action-surface (F9,F13,F14) · nav/first-run/microcopy: zero.

Confirmed-fixed: **none.** (No round-1 UX finding was resolved before the recovery checkpoint.) D28 (mobile root ErrorBoundary + web (chat) error.tsx) verified absent. The staged `i18n-additions/chat.json` was never merged.

Visual confirmation of states remains user-owned (DEF-1); all 16 are static-code evidence with both platform files named.
