# Sweep 9 — Round 2: test coverage & quality (issue #107)

Read-only re-audit. Method: read round-1 report (`round-1/09-tests.md`) + triage register + `recovery-notes.md`, then VERIFIED each round-1 gap against the CURRENT trees (ui-mobile @ main, orbit-api @ chore/107-code-health-sweep) by reading the actual test bodies (not filenames/grep substrings), plus audited NEW logic introduced by round-1 fixes (shared DRY cores, new validators, error-code refactor) for behavior coverage. Suites not run (declared green). Integration/E2E absence not reported (deliberate).

Critical context from `recovery-notes.md`: **Wave-2 frontend agents died mid-edit at the session limit; a recovery pass restored green build/test but most wave-2 *test additions* never landed.** orbit-api wave-1/1.5 (incl. error-code refactor + new backend tests) DID complete (`eee06ae`). Net effect verified below: orbit-api round-1 gaps are mostly CLOSED; frontend round-1 gaps are mostly STILL OPEN; and two shared DRY cores were created+tested but never wired into the real hooks (orphaned).

## Confirmed FIXED since round 1 (do not re-report)

- `GoalStreakSyncService` — `tests/Orbit.Application.Tests/Services/GoalStreakSyncServiceTests.cs` (198 lines) invokes real `NeedsPassiveSync`/`CalculateCurrentStreak`/`SyncCurrentStreak[IfNeeded]`; covers same-day no-resync, stale resync, never-synced, Min-across-habits, zero-habits. CLOSED.
- `ExportUserDataQueryHandler` — `tests/Orbit.Application.Tests/Queries/Profile/ExportUserDataQueryHandlerTests.cs` (186 lines): user-not-found, empty account, log ordering/grouping, pro/free plan, multi-repo aggregation. CLOSED.
- The 5 tautological `HabitDueDateAdvancementServiceTests` — production extracted `StaleBadHabitFilter(cutoff)` (Expression) + `ShouldAdvanceForUserToday(habit, userToday)`; the 5 tests now invoke those real seams (`StaleBadHabitFilterFor(cutoff)(habit).Should()...`, `HabitDueDateAdvancementService.ShouldAdvanceForUserToday(...).Should()...`). No longer tautological. CLOSED.
- New validators all have valid+invalid-branch tests under `tests/Orbit.Application.Tests/Validators/`: BulkDeleteUserFacts, ConfirmAccountDeletion, GetAllHabitLogs, GetCalendarMonth (the >62-day guard moved from handler → validator, tested at the >62 and exactly-62 boundaries), GetDailySummary, UnsubscribePush. CLOSED.
- `GamificationService`/`GetStreakInfoQueryHandler` new on-read streak recalculation — `tests/Orbit.Application.Tests/Queries/Gamification/GetStreakInfoQueryHandlerTests.cs::Handle_RecalculatesStreakOnRead_AndReturnsFreshValues` asserts `RecalculateAsync(..., awardFreezeIfEligible:false)` + persisted + fresh values returned. CLOSED.
- Shared DRY cores that ARE wired into production (1–5 real importers each) and behavior-tested: `drill-navigation` (loadDrillChildren/mergeDrillChildrenMap, incl. multi-level + merge overlap + error propagation), `breakdown-suggestion`, `pending-notification-deletes` (queue/cancel/subscribe lifecycle + throw-clears-pending), `highlight-text` (no-match/multi/case-insensitive; uses indexOf so regex-escaping is moot), `habit-optimistic` (withChildren hasSubHabits matrix + recursive optimisticPatchHabit), `advanced-settings`, `profile-navigation`, `api-key-form::parseApiKeyExpiryUtc` (exhaustive valid/invalid/out-of-range UTC parsing). CLOSED.
- `extractBackendErrorCode` (new) + `getFriendlyErrorKey` errorCode→i18n mapping — covered in `packages/shared/src/__tests__/error-utils-extended.test.ts` (the `40 -` in the diff was the `local/no-comments` autofix stripping `// ----` dividers, not test deletions; verified via `git diff`). CLOSED.

## STILL-OPEN findings

- HIGH · `apps/mobile/lib/google-auth.ts` (signInWithGoogle, exchangeGoogleSession, mergeRequestIdIntoPayload) · Round-1 HIGH gap NOT filled — no `apps/mobile/__tests__/lib/google-auth.test.ts` exists; only the sibling `google-auth-callback` is tested. Searched `apps/mobile/__tests__/lib/`. Only Google login path on Android, still zero coverage. · Suggested: unit-test `mergeRequestIdIntoPayload` (null / non-object / existing-requestId) and `exchangeGoogleSession` success + non-OK error shaping with stubbed `fetch`; WebBrowser result-type branches with mocked `expo-web-browser`.

- HIGH · `apps/web/app/api/auth/{google,send-code,verify-code,session,logout}/route.ts` + `apps/web/lib/auth-proxy.ts` · Round-1 HIGH gap NOT filled — no test in `apps/web/__tests__` imports any auth route or `lib/auth-proxy` (the `app/api/auth/` test dir does not exist; catch-all `proxy-route.test.ts` + `lib/auth-api.test.ts` cover different files). The token-stripping cookie boundary ("sets httpOnly auth+refresh cookies, returns user without the token") is untested. Additionally, D23 (Zod-parse request bodies at the boundary; log server-side on 500) was NOT implemented — routes still cast body to `unknown` with no boundary validation, so there is no new validation logic to test yet either. · Suggested: route tests mirroring `subscriptions-checkout-route.test.ts` (mock fetch; assert cookies set, token absent from body, status/error passthrough) + a small `auth-proxy.test.ts` over its pure helpers.

- HIGH · `apps/web/hooks/use-dismiss-guard.ts` + `apps/mobile/hooks/use-dismiss-guard.ts` (useDismissGuard) AND orphaned `packages/shared/src/hooks/dismiss-guard-core.ts` · Round-1 created `resolveDismissGuardAction` + a passing `dismiss-guard-core.test.ts`, but the real web/mobile hooks were NEVER rewired to consume it — both still contain the original byte-identical inline `useState`/`useCallback` state machine (verified: `grep resolveDismissGuardAction` over `apps/` + shared non-test = **0 production importers**). So (a) the round-1 "zero tests on BOTH platforms" gap is STILL OPEN for the shipping hooks, and (b) the tested shared core is dead code testing a path that never executes — a false sense of coverage. · Suggested: rewire both wrappers to delegate to `resolveDismissGuardAction` (then the core test covers the logic and the wrappers need only a thin render smoke test), OR add a renderHook spec per platform (clean→dismiss, dirty→dialog, confirm, cancel). Do not leave the orphaned core + duplicated untested hooks.

- MED · `apps/mobile/hooks/use-tag-selection.ts` (223 lines) AND orphaned `packages/shared/src/hooks/tag-selection-core.ts` (242 lines, 319-line test) · Same orphan pattern: the 319-line `tag-selection-core.test.ts` exercises a core with **0 production importers** (`grep` over `apps/` non-test for `createInitialTagSelectionState`/`toggleTagSelection`/`createAndSelectTagFlow` = 0). Both `apps/mobile/hooks/use-tag-selection.ts` (223 L) and `apps/web/hooks/use-tag-selection.ts` (225 L) still re-implement the full React state machine inline and have zero mobile/web React-level tests (mobile symbol appears only in `vi.mock` stubs in `create-habit-modal.test.tsx`). The round-1 mobile-`use-tag-selection` gap is STILL OPEN; the core test does not reach the shipping code. · Suggested: rewire both hooks to the shared core (then add a thin renderHook port), or test the mobile hook directly (toggle add/remove, create-and-select, limit, delete-rollback).

- MED · `apps/mobile/app/(tabs)/profile.tsx` (account-deletion state machine: deleteStep `confirm`→`code`→`deactivated`, openDeleteModal, backToDeleteConfirmStep, 6-digit code entry, loading/error) · Round-1 MED gap NOT filled — no `delete`/`deleteStep`/`deactivated` assertions in `apps/mobile/__tests__/screens/profile-screen.test.tsx` (only `intentional-offline-ux.test.tsx` references the symbol, for offline gating). Web twin `apps/web/__tests__/components/profile/delete-account-modal.test.tsx` exists. Mobile is the Play-shipped, compliance-primary delete path. · Suggested: mirror the web modal test — step transitions, code-submit error, deactivated end state.

- MED · `apps/mobile/hooks/use-login-code-entry.ts` (useLoginCodeEntry) · Round-1 MED gap NOT filled — no `apps/mobile/__tests__/hooks/use-login-code-entry.test.ts`; web twin exists. Mobile-specific: TextInput ref focus-advance, multi-char paste fill, backspace-to-previous, 60s `setInterval` resend countdown + cleanup — the exact OTP area that regressed in 8c9906f. · Suggested: renderHook with fake timers (countdown/cleanup) + paste/advance + backspace cases.

- MED · `apps/mobile/hooks/use-chat-reward.ts` (useChatReward) · Round-1 MED gap NOT filled — no test references the symbol as unit under test. Rewarded-ad → `POST API.subscription.adReward` → patches cached `Profile.aiMessagesLimit`; gate via `canWatchRewardAd`. Mutates a monetization-relevant cache. `use-ad-mob` is tested; the orchestration on top is not. · Suggested: hook test mocking `useAdMob` + `apiClient` — reward-earned grants & patches cache; not-earned → failure message; API throw → failure; gate short-circuits.

- MED · `apps/mobile/hooks/use-review-reminder.ts` (shouldShow gating matrix + requestReview StoreReview→Play-URL fallback) · Round-1 MED gap NOT filled — only `stores/review-reminder-store.test.ts` (store) exists; the hook's gating (onboarding done, completionCount ≥ 20, activeDays ≥ 5, acceptedAt, `dismissedUntil >= today` string compare, foreground, celebration suppression) and StoreReview→Linking fallback chain are untested. · Suggested: renderHook matrix over store states + mocked `expo-store-review`/`Linking`.

- MED · `apps/mobile/hooks/` mirrors with zero mobile coverage: `use-habit-queries.ts`, `use-goal-queries.ts`, `use-summary.ts`, `use-calendar-data.ts`, `use-habit-form.ts`, `use-resolve-clarification.ts` · Round-1 MED gap NOT filled — each has a web test twin but no mobile test exercises the real mobile hook (searched `apps/mobile/__tests__/hooks/`). Highest priority: `use-habit-queries.ts:82-87` single-day `refetchInterval` gating on `isAppActive()`/`isOnline()` (midnight-rollover polling) is mobile-only logic asserted nowhere; `use-resolve-clarification` success-gated invalidation set likewise. Shared cores (habit-query/pagination/habit-form-state) are tested in `packages/shared`, but the mobile-specific wiring is not. · Suggested: prioritize a mobile `use-habit-queries` test (refetchInterval gate + pagination fan-out with mocked `apiClient`); the rest can be thin renderHook ports of the web suites.

- MED · `orbit-api src/Orbit.Api/Extensions/ResultActionResultExtensions.cs` (`ToErrorResult`, `ToErrorBody`) — NEW logic from the D3 backend refactor · The whole point of the refactor is that every failure body now carries `{ error, errorCode }` (line 39) — but `ResultActionResultExtensionsTests` asserts **only `.StatusCode`** (400/403); it never asserts `ObjectResult.Value` contains both `error` and `errorCode`. A regression dropping `errorCode` from the payload would pass every test. Separately, `ToErrorBody(this AppError)` (the controller-authored-failure path, line 48) has **zero** test references (no test constructs an `AppError` and calls it). · Suggested: in `ResultActionResultExtensionsTests`, assert the `ObjectResult.Value` shape — `error == result.Error` AND `errorCode == result.ErrorCode` (e.g. via reflection or an anonymous-type read) for a normal failure and a pay-gate failure; add a `ToErrorBody` test (`AppError.Code`/`.Message` → `{ error, errorCode }`).

- LOW · `orbit-api tests/Orbit.Application.Tests/Common/ErrorCodesAndMessagesTests.cs` · The catalog test asserts format (UPPER_SNAKE_CASE), non-empty, no-duplicate — but does NOT assert code↔message coherence (every `ErrorCodes` constant has a corresponding `ErrorMessages` `AppError`, and that `AppError.Code` matches its `ErrorCodes` value). With the new `AppError`-based catalog, a code/message mismatch (wrong `Code` baked into an `AppError`) is uncaught. · Suggested: add a test iterating `ErrorMessages` `AppError` fields asserting each `.Code` exists in `ErrorCodes` (and optionally that every `ErrorCodes` value is referenced by some `AppError`).

- LOW · `apps/mobile/components/ui/create-api-key-modal.tsx` · The new shared validators `MAX_API_KEY_NAME_LENGTH` + `parseApiKeyExpiryUtc` are imported and used only by the WEB modal (`apps/web/components/ui/create-api-key-modal.tsx`, tested by `create-api-key-modal.test.tsx`); the mobile modal exists but does NOT import them (`grep MAX_API_KEY_NAME_LENGTH|parseApiKeyExpiryUtc apps/mobile/components` = 0) and has NO test on either count. (Parity itself is sweep #2's call; the test-coverage angle: mobile api-key name-length/expiry validation is unexercised.) · Suggested: once mobile adopts the shared validators, add a mobile `create-api-key-modal` test mirroring web (name-too-long + invalid-expiry branches). `MAX_API_KEY_NAME_LENGTH` (new constant) also has no direct assertion in `packages/shared/src/__tests__` — a one-line value/usage test would close it, though it's exercised transitively via the web modal.

- LOW · `apps/web/__tests__` class-name / selector assertions (~10 files) · Round-1 LOW gap NOT filled — spot-checked and confirmed still asserting Tailwind classes/CSS selectors rather than behavior: `pages/login.test.tsx:108-109,141` (`toHaveClass('max-w-[26rem]')`), `components/ui/skeleton.test.tsx` (multiple `toHaveClass`), `pages/streak.test.tsx:106` (`querySelector('.streak-hero__count')`), `components/goals/goal-card.test.tsx:77` + `components/habits/habit-checklist.test.tsx:124` (`className.toContain('line-through')`), `components/goals/goal-list.test.tsx` (`drag-chosen`/`drag-ghost`), plus `chat/message-bubble.test.tsx`, `ui/markdown.test.tsx`, `ui/app-overlay.test.tsx`, `onboarding/onboarding-flow.test.tsx`. Violates apps/web/CLAUDE.md "assert on behavior + data attributes, NOT class names." · Suggested: replace with role/test-id/data-attribute queries (`data-completed`, accessible text) where the visual state is load-bearing.

- LOW · `orbit-api` reflection / private-member tests (unchanged from round 1) · Still present: six AI service test files invoke private statics via `BindingFlags.NonPublic` (`BuildExtractionPrompt`, `BuildHistoryTranscript`, `NormalizeSchemaTypes`, `ConvertToSdkTool`, `BuildRetrospectivePrompt`, `BuildSummaryPrompt`, `SelectScheduledHabits`, `GenerateFallback`), and `GetHabitByIdQueryHandlerTests` writes the private `Habit._children`. Renaming a private breaks tests with no behavior change. · Suggested: assert prompts at the mocked AI-client boundary; build the parent/child graph through the public `Habit` API.

- LOW · hand-rolled `NormalizedHabit` fixtures (unchanged from round 1) · 12+ files still duplicate ~35-field habit literals instead of `packages/shared/src/__tests__/factories.ts#createMockHabit` (e.g. `apps/web/__tests__/hooks/use-habit-visibility.test.ts`, `apps/mobile/__tests__/hooks/use-habits.test.ts`, `packages/shared/src/__tests__/habit-visibility.test.ts`). Note round-1 trimmed `factories.ts` by 21 lines, widening drift risk. · Suggested: replace local `makeHabit` helpers with `createMockHabit(overrides)`.

- LOW · `apps/mobile/lib/offline-runtime.ts`, `apps/mobile/hooks/use-horizontal-swipe.ts`, `apps/web/hooks/use-date-format.ts` + mobile twin, `apps/web/hooks/use-calendar-events.ts` (unchanged from round 1) · Still only ever mocked, never exercised: offline connectivity resolution (`isConnected && isInternetReachable` + cached-fallback-on-throw), gesture threshold/direction math, date-format null-guard/locale wiring, calendar-events wrapper. Underlying date math is covered in `time-format.test.ts`; these are wrapper/edge risks. · Suggested: small unit tests where cheap (offline 4-case; swipe threshold/direction); accept the thin date/calendar wrappers as low risk.

## Cross-cutting note (NOT a test finding — flagged for the round-2 fix pass)

- D3 client-half is confirmed UNFINISHED (matches `recovery-notes.md`): the backend now emits stable `errorCode`s, and the client added `extractBackendErrorCode` + a `getFriendlyErrorKey` errorCode→i18n path (both tested), BUT `getErrorMessage` (`error-utils.ts:170`) still returns the raw backend string and the primary message resolution (`CONTEXTUAL_RULES`, line 214+) still matches on English text fragments rather than codes. No `error-codes.json` catalog was emitted. This is additive/non-breaking (clients still read `error`), so it is an unfinished IMPROVEMENT, not a coverage gap with an uncovered symbol — recorded here so the fix pass decides complete-vs-defer, per the recovery note.

## Verdict

15 STILL-OPEN findings: **3 HIGH, 6 MED, 6 LOW**. Plus 1 cross-cutting fix-pass note (D3 client-half, not a coverage finding). Confirmed-fixed (not re-reported): all 5 orbit-api round-1 gaps (GoalStreakSync, ExportUserData, the tautology pack, the 6 new validators, GamificationService streak-recalc) and 8 of the 10 new shared DRY cores.

Dominant theme: orbit-api round-1 fixes landed and are well-tested; the FRONTEND wave-2 test additions mostly did NOT land (session-limit death), so the mobile-hooks / BFF-auth / google-auth / account-deletion gaps persist verbatim. Two shared cores (`dismiss-guard-core`, `tag-selection-core`) were created+tested but never wired into the real hooks — orphaned coverage that masks the still-untested shipping hooks. One genuinely new backend gap: the D3 error-payload shape (`{ error, errorCode }`) and `ToErrorBody` are emitted but unasserted.
