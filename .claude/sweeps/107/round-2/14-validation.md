# Sweep #14 — Frontend Validation Parity (Issue #107, Strand 5, ROUND 2)

Date: 2026-06-12. Working tree of `orbit-ui-mobile` @ main (baseline ae5c150). Read-only audit. Verifies round-1 findings (`round-1/14-validation.md`) against current code after round-1 fix agents (which died mid-work).

**Source of truth:** `packages/shared/src/validation/{constants.ts, habit-form.ts, goal-form.ts, tag-form.ts, api-key-form.ts, index.ts}` + shared helpers in `utils/`.

## What round-1 actually landed (confirmed-fixed in the SHARED layer)

- `MAX_API_KEY_NAME_LENGTH = 50` added to `constants.ts:17` and exported from `validation/index.ts:2`.
- `parseApiKeyExpiryUtc(value)` added as a new module `validation/api-key-form.ts` (one strict `YYYY-MM-DDTHH:MM[:SS]` pattern, UTC decision) and exported from `validation/index.ts:48`.
- `validateScheduledReminders` + `MAX_SCHEDULED_REMINDERS` already exported from `validation/index.ts:15,32`.
- All length/cap constants exist and are exported (`constants.ts:1-17`, `index.ts:1-19`).

The shared building blocks for findings 3, 5, 13 are now present. The **consumption** of those blocks landed on web only for the API-key surface; everywhere else the platform literals are unchanged. The two HIGH submit-gating findings (1, 2) are untouched.

## Per-surface comparison table (round-2 state)

| Surface | 1 — Required + submit gating | 2 — Lengths (shared const?) | 3 — Formats | 4 — Conditional | 5 — Error keys | 6 — Single source | Status |
|---|---|---|---|---|---|---|---|
| **Habit create** | **DIVERGES (unchanged).** Web `!formState.isValid` (live zodResolver) — `create-habit-modal.tsx:360`. Mobile `!titleFilled` — `create-habit-modal.tsx:313`. Zero/empty frequency → web silently disables (no inline reason), mobile stays enabled + toasts `frequencyRequired`. | Title `maxLength={200}` LITERAL both (web `habit-form-fields.tsx:1011` / mobile `title-section.tsx:39`); desc `2000` LITERAL both (web `:1349` / mobile `advanced-section.tsx:97`). | time/date shared `validateTime` at submit. OK | all shared via `validateAll`. OK | shared keys on both. | gating local. | **F1 OPEN (HIGH)** + F6,F7 |
| **Habit edit** | **DIVERGES (unchanged).** web `!formState.isValid` `edit-habit-modal.tsx:183` vs mobile `!titleFilled` `edit-habit-modal.tsx:199`. | same literals as create. | same. OK | shared. OK | shared. | gating local. | **F2 OPEN (HIGH)** |
| **Sub-habit editor** | **DIVERGES (unchanged).** Web hides editor entirely for non-pro (`create-habit-modal.tsx:271-339` ternary → upsell only). Mobile renders editable rows for non-pro (`sub-habit-editor.tsx:52-99`, only adds hint+badge) AND re-routes typed-sub-habit submit to `/upgrade` (`create-habit-modal.tsx:238-245`) — no web counterpart. | Row `maxLength={200}` + cap `>= 20` LITERAL both (web `:288,307`; mobile `sub-habit-editor.tsx:64,91`). | n/a | shared `validateSubHabits` at submit. OK | shared keys. | caps local. | **F4 OPEN (MED)** + F8 |
| **Goal create** | Both gate on `isSubmitting` only. OK | desc `MAX_GOAL_DESCRIPTION_LENGTH` imported both (web `:330`, mobile `:230`). Unit `maxLength={50}` LITERAL both (web `:312`, mobile `:322`). | Target: web `min={0.01}` native (browser bubble pre-empts) `create-goal-modal.tsx:502` + form has NO `noValidate` `:231`; mobile `decimal-pad` + shared msg. | shared `validateGoalDraftInput`. OK | shared keys; web native-bubble pre-empts. | shared helper. | **F10 OPEN (MED)** + F14 |
| **Goal edit** | Both `isSubmitting` only. OK | desc constant both (web `:213`, mobile `:198`); unit `50` LITERAL both (web `:199`, mobile `:237`). | web `min=0.01` `:372` + form no `noValidate` `:157`; mobile decimal-pad. | shared. OK | shared keys. | shared helper. | **F10 OPEN (MED)** + F14 |
| **Goal progress** | shared `validateGoalProgressInput` both. OK | no note cap either (equal). | OK | ≥0 shared. OK | shared keys. | shared. | compliant |
| **Tag create/edit** | shared `validateTagForm` both. OK | name `maxLength={50}` LITERAL both (web `habit-form-fields.tsx:150`, mobile `tag-editor-row.tsx:38`). | `HEX_COLOR_PATTERN` shared. OK | n/a | shared keys. | shared validator. | **F9 OPEN (MED)** |
| **Checklist items** | add-disabled on empty trim both; caps submit-only both. OK | shared submit-check only (equal). | n/a | shared `validateChecklistItems`. OK | shared keys. | shared. | compliant |
| **Checklist templates** | identical gate. OK | equal (no const). | n/a | n/a | shared keys. | n/a | compliant |
| **Reminders — offset** | identical (`length<=1`, `HABIT_REMINDER_PRESETS`). OK | n/a | int minutes both. OK | shared `validateReminderSelection` + `resolveAutoManagedReminderEnabled`. OK | shared keys. | shared. | compliant |
| **Reminders — scheduled** | **EQUAL but both re-impl (unchanged).** Both re-declare `const MAX_SCHEDULED_REMINDERS = 5` (web `habit-form-fields.tsx:627`, mobile `scheduled-reminder-section.tsx:35`) + inline limit/dup/empty pre-add checks (web `:632-648`, mobile `:40-58`) instead of importing the constant + calling `validateScheduledReminders`. Shared validator still runs at submit. | local `5` LITERAL both. | shared `AppTimePicker`. OK | shared at submit. OK | same keys. | local re-impl (equal). | **F5 OPEN (MED)** |
| **Due/end date+time** | shared conditionals both. OK | n/a | shared `validateTime`. OK | shared `validateEndDate`/`validateEndTime`. OK | shared keys. | shared. | compliant |
| **Auth — email** | web `email-step.tsx:53` / mobile adds `isOnline` (platform adapter). shared `isValidEmail` both. OK | n/a | shared `isValidEmail`. OK | n/a | `auth.errors.invalidEmail` both. | shared. | compliant |
| **Auth — magic code** | gate on 6 digits both; shared `VERIFICATION_CODE_LENGTH` adopted in the entry hooks (web `use-login-code-entry.ts:71,83,98,104`, mobile `use-login-code-entry.ts:77`). Literal `6` remains in the gating/complete guards (web `use-login-flow.ts:118` + `code-step.tsx:86`; mobile `login.tsx:299,403`) — **EQUAL on both** → drift, not divergence. | `6` literals (equal). | digits-only shared. OK | n/a | shared keys. | mostly shared; gate literals equal. | **F12 OPEN (LOW, drift)** |
| **Profile — display name** | `setNameRequestSchema.safeParse` both (web `edit-name-sheet.tsx:61-71`, mobile `:70-80`). | shared schema. OK | n/a | n/a | identical keys. | shared schema. | compliant |
| **Profile — tz/week/lang** | closed pickers from shared option lists. OK | n/a | n/a | n/a | n/a | shared. | compliant / n-a |
| **Profile — AI memory/summary** | toggles only. n/a | n/a | n/a | n/a | n/a | n/a | compliant / n-a |
| **Support / contact** | identical gate + `validateForm` + shared `isValidEmail`. OK | no caps (equal). | shared `isValidEmail`. OK | shared `buildSupportRequestBody`. OK | identical keys. | shared. | compliant |
| **API key creation** | both `isSubmitting` only. **EXPIRY DIVERGES — now ASYMMETRICALLY FIXED.** Web fully adopted shared: `parseApiKeyExpiryUtc` in validate `create-api-key-modal.tsx:75` + submit `:99`, error key `orbitMcp.invalidExpiry` `:76`, `MAX_API_KEY_NAME_LENGTH` `:70,221`. Mobile UNCHANGED: lenient `new Date(expiresAt)` `:104,131`, wrong error key `auth.genericError` `:106`, name `>50` LITERAL `:99` + `maxLength={50}` `:236`. | web const; mobile `50` LITERAL ×2. | **DIVERGES** (web strict-UTC, mobile lenient-local). | n/a | **DIVERGES** (web `orbitMcp.invalidExpiry`, mobile `auth.genericError`). | web shared helper; mobile local. | **F3 OPEN (HIGH, now worse)** + F13 |
| **Onboarding habit create** | identical `!title.trim()` gate + shared `validateHabitFormInput`. OK | title `maxLength={200}` LITERAL both (web `onboarding-create-habit.tsx:182`, mobile `:195`). | n/a | shared. OK | shared keys. | shared validator. | **F6 OPEN (MED)** |

## Findings (round-2 — all verified STILL DIVERGENT)

1. **HIGH · STILL OPEN (unchanged)** · web `apps/web/components/habits/create-habit-modal.tsx:360` + mobile `apps/mobile/components/habits/create-habit-modal.tsx:313` · dim 1 (submit gating) · Web gates submit on full `formState.isValid` (live zodResolver); mobile gates on `titleFilled` only. With emptied/zero frequency quantity, web silently disables the button (no rendered reason) while mobile keeps it enabled and toasts `habits.form.frequencyRequired`. This is the exact class named in issue #107. Fix: one gating contract for both — extract `isHabitFormSubmittable(values)` in `@orbit/shared/validation` (or gate both on title presence, letting `validateAll`'s toast carry the rest) and import on both.
2. **HIGH · STILL OPEN (unchanged)** · web `apps/web/components/habits/edit-habit-modal.tsx:183` + mobile `apps/mobile/components/habits/edit-habit-modal.tsx:199` · dim 1 · Same gating divergence in the edit modal (`!formState.isValid` vs `!titleFilled`). Same fix as F1.
3. **HIGH · STILL OPEN — now asymmetrically regressed** · web `apps/web/components/ui/create-api-key-modal.tsx:75,99,76` + mobile `apps/mobile/components/ui/create-api-key-modal.tsx:104-108,131` · dim 3+5 (format + error key) · Round-1 added the shared `parseApiKeyExpiryUtc` and web fully adopted it (strict UTC pattern, error key `orbitMcp.invalidExpiry`). Mobile was NOT migrated: still lenient `new Date(expiresAt)` parsed as local time (`:104`, `:131`) and still surfaces the wrong key `t('auth.genericError')` (`:106`) instead of `orbitMcp.invalidExpiry`. The gap is now wider than round-1: web rejects strings mobile accepts, the same string yields an `expiresAtUtc` shifted by the device offset, AND the error message differs. Fix: import `parseApiKeyExpiryUtc` in the mobile modal, use it in `validate()` and the submit `expiresAtUtc` build, and switch the error to `t('orbitMcp.invalidExpiry')`.
4. **MED · STILL OPEN (unchanged)** · web `apps/web/components/habits/create-habit-modal.tsx:271-339` + mobile `apps/mobile/components/habits/create-habit-modal.tsx:238-245` + `apps/mobile/components/habits/create-habit-modal/sub-habit-editor.tsx:52-99` · dim 1+4 · Non-pro sub-habit flow differs: web hides the editor for non-pro entirely; mobile renders editable rows (only adds a hint + ProBadge at `sub-habit-editor.tsx:50-55`) and re-routes a submit with typed sub-habits to `/upgrade` via `hasTypedSubHabits` (`create-habit-modal.tsx:238-245`), which has no web counterpart. Fix: mirror one presentation (hide on both, or upsell-rows + redirect on both) and delete the asymmetric branch.
5. **MED · STILL OPEN (unchanged)** · web `apps/web/components/habits/habit-form-fields.tsx:627,632-648` + mobile `apps/mobile/components/habits/habit-form-fields/scheduled-reminder-section.tsx:35,40-58` · dim 2+6 · Both still re-declare `const MAX_SCHEDULED_REMINDERS = 5` locally and re-implement the add-time limit/duplicate/empty checks inline instead of importing `MAX_SCHEDULED_REMINDERS` + calling the now-exported `validateScheduledReminders([...current,{when,time}])`. Equivalent today; pure drift risk. Fix: import both from `@orbit/shared/validation`; validate the candidate list and map the returned key to the pre-add toast.
6. **MED · STILL OPEN (unchanged)** · web `apps/web/components/habits/habit-form-fields.tsx:1011` + `apps/web/components/onboarding/onboarding-create-habit.tsx:182` + mobile `apps/mobile/components/habits/habit-form-fields/title-section.tsx:39` + `apps/mobile/components/onboarding/onboarding-create-habit.tsx:195` · dim 2 · Habit title `maxLength={200}` literal in all four; `MAX_HABIT_TITLE_LENGTH` exists + exported. Fix: import the constant in all four.
7. **MED · STILL OPEN (unchanged)** · web `apps/web/components/habits/habit-form-fields.tsx:1349` + mobile `apps/mobile/components/habits/habit-form-fields/advanced-section.tsx:97` · dim 2 · Habit description `maxLength={2000}` literal; `MAX_HABIT_DESCRIPTION_LENGTH` exists. Fix: import the constant on both.
8. **MED · STILL OPEN (unchanged)** · web `apps/web/components/habits/create-habit-modal.tsx:288,307` + mobile `apps/mobile/components/habits/create-habit-modal/sub-habit-editor.tsx:64,91` · dim 2 · Sub-habit input `maxLength={200}` and add-cap `>= 20` literals; `MAX_HABIT_TITLE_LENGTH` / `MAX_SUB_HABITS` exist. Fix: import the constants on both.
9. **MED · STILL OPEN (unchanged)** · web `apps/web/components/habits/habit-form-fields.tsx:150` + mobile `apps/mobile/components/habits/habit-form-fields/tag-editor-row.tsx:38` · dim 2 · Tag name `maxLength={50}` literal; `MAX_TAG_NAME_LENGTH` exists. Fix: import the constant on both.
10. **MED · STILL OPEN (unchanged)** · web `apps/web/components/goals/create-goal-modal.tsx:312` + `apps/web/components/goals/edit-goal-modal.tsx:199` + mobile `apps/mobile/components/goals/create-goal-modal.tsx:322` + `apps/mobile/components/goals/edit-goal-modal.tsx:237` · dim 2 · Goal unit `maxLength={50}` literal in all four (the adjacent description field already imports `MAX_GOAL_DESCRIPTION_LENGTH`); `MAX_GOAL_UNIT_LENGTH` exists. Fix: import the constant in all four.
11. **MED · STILL OPEN (unchanged)** · web `apps/web/components/habits/create-habit-modal.tsx:92` + `apps/web/components/habits/edit-habit-modal.tsx:58` + mobile `apps/mobile/components/habits/create-habit-modal.tsx:111` + `apps/mobile/components/habits/edit-habit-modal.tsx:66` · dim 2 · `atGoalLimit = selectedGoalIds.length >= 10` literal in all four modals; `MAX_GOALS_PER_HABIT` exists (and `validateGoalSelection` already uses it at submit). Fix: import the constant in all four.
12. **LOW (drift, not divergence) · PARTIALLY adopted** · web `apps/web/app/(auth)/login/use-login-flow.ts:118` + `apps/web/app/(auth)/login/code-step.tsx:86` + mobile `apps/mobile/app/login.tsx:299,403` · dim 2 · `VERIFICATION_CODE_LENGTH` is now adopted in the shared code-entry hooks on BOTH platforms (`use-login-code-entry.ts`), but the submit-gating / completion guards still hardcode `6`. The literal is identical on both platforms, so this is a single-source drift risk, not a behavioral parity gap. Fix: replace the four literal `6`s with `VERIFICATION_CODE_LENGTH` (and `isVerificationCodeComplete` where the join-length check is used).
13. **MED · STILL OPEN on mobile only (web fixed)** · mobile `apps/mobile/components/ui/create-api-key-modal.tsx:99,236` · dim 2 · API-key name cap: web now imports `MAX_API_KEY_NAME_LENGTH` (`create-api-key-modal.tsx:70,221`); mobile still uses literal `50` in the validate guard (`:99`) and `maxLength={50}` (`:236`). Fix: import `MAX_API_KEY_NAME_LENGTH` in the mobile modal (paired with the F3 expiry fix).
14. **LOW · STILL OPEN (unchanged)** · web `apps/web/components/goals/create-goal-modal.tsx:231` (form, no `noValidate`) + `:502` (`min={0.01}`) and `apps/web/components/goals/edit-goal-modal.tsx:157,372` + mobile `apps/mobile/components/goals/create-goal-modal.tsx:294-307` · dim 5 · For a 0/negative target, web's native `min=0.01` inside a validating form blocks submit with an untranslated browser bubble before the shared `goals.form.targetValueRequired` message can surface; mobile always shows the shared i18n message. Fix: add `noValidate` to the web goal forms (or drop the native `min`, keep `step`) so the shared validator owns the message on both.

## Verdict

**14 findings — 3 HIGH · 9 MED · 2 LOW.** (Same 14 as round-1; F12 confirmed downgraded to LOW-drift after partial hook adoption; F13 narrowed to mobile-only after web fix.)

- **NONE of the 14 round-1 findings is fully resolved.** Round-1 produced the shared scaffolding (constants, `parseApiKeyExpiryUtc`, `validateScheduledReminders`, `MAX_API_KEY_NAME_LENGTH`) but the per-platform adoption (W2 fix agents) did not complete.
- **Confirmed-fixed in shared layer only:** the building blocks for F3/F5/F13 now exist and are exported; web adopted the API-key pieces (F3 expiry + F13 name). Mobile did not — so F3 is now an *asymmetric* divergence (web strict-UTC + `orbitMcp.invalidExpiry`; mobile lenient-local + `auth.genericError`), arguably worse than the round-1 symmetric-but-both-wrong state.
- **The two HIGH submit-gating findings (F1, F2) are byte-for-byte unchanged** — web `formState.isValid` vs mobile `titleFilled` in both create and edit modals.

### Top 10 (by leverage)
1. **F1** habit-create submit gating (HIGH) — `create-habit-modal.tsx:360` web vs `:313` mobile.
2. **F2** habit-edit submit gating (HIGH) — `edit-habit-modal.tsx:183` web vs `:199` mobile.
3. **F3** API-key expiry parse + error key (HIGH, mobile not migrated) — mobile `create-api-key-modal.tsx:104-108,131,106`.
4. **F4** sub-habit non-pro flow asymmetry (MED) — web hides vs mobile rows+redirect.
5. **F13** API-key name cap (MED, mobile-only) — mobile `create-api-key-modal.tsx:99,236`.
6. **F5** scheduled-reminders local re-impl (MED) — web `habit-form-fields.tsx:627`, mobile `scheduled-reminder-section.tsx:35`.
7. **F11** goals-limit `>=10` ×4 (MED).
8. **F10** goal-unit `50` ×4 (MED).
9. **F6/F7/F8/F9** habit title/desc/sub-habit/tag length literals (MED).
10. **F14** goal-target native `min` bubble on web (LOW).

### Confirmed-fixed note
Round-1 fixes that DID land and are verified correct: the shared `validation/api-key-form.ts` module (`parseApiKeyExpiryUtc`, one pattern, UTC), `MAX_API_KEY_NAME_LENGTH` constant + export, and the **web** API-key modal's full adoption of both (strict expiry + `orbitMcp.invalidExpiry` + name constant). `VERIFICATION_CODE_LENGTH` adoption in both platforms' `use-login-code-entry.ts` hooks. No regressions introduced elsewhere; every other validated surface (goal progress, checklist, offset reminders, due/end date-time, auth email, display name, support) remains compliant and identical on both platforms.
