# Sweep #12 — UX states + copy (issue #107, ROUND 3 verification)

Read-only static re-audit at committed-green HEAD `6399d00` (wave-3 + i18n central merge), 2026-06-13. Verifies the round-2 set (F1–F16) against current code with file:line evidence.

NOTE: round-2 was written against the recovery commit `ae5c150`, where "no sweep-12 partition was applied." HEAD `6399d00` re-landed the UX fixes plus the central i18n merge (commit 9bdcdff: `chat.offline.*`, `errors.offline`, `errors.bulkCreateHabits`, breakdown keys in both locales). Verified against current code.

## Round-2 verification matrix

| R2 | Domain | Verdict now | Evidence |
|---|---|---|---|
| F1 | web drill error unrendered | **STILL OPEN** | `habit-list/drill-content.tsx` has no `drillError` prop/branch — failed fetch falls to "No sub-habits yet + Add" empty (71-94); hook still sets `drillError` (`use-drill-navigation.ts:67,92`) but no web consumer; mobile renders it |
| F2 | mobile profile raw error.message | **RESOLVED** | `profile.tsx:542-544` now `__DEV__ && error instanceof Error ? error.message : t('errors.loadProfile')` — dev-gated like web |
| F3 | mobile create-key raw err.message | **RESOLVED** | `advanced.tsx:197` `setCreateKeyError(t('orbitMcp.createKeyError'))` — friendly key, aligned with web |
| F4 | web fetchApiKeys swallows !ok | **RESOLVED** | `advanced/page.tsx:48-50` now `throw new Error('Failed to load API keys')`; error branch reachable |
| F5 | user-facts load error masked | **RESOLVED** | web fetcher `use-user-facts.ts:13-15` throws on `!ok`; `user-facts-list.tsx:90` renders `t('profile.facts.factsError')` + retry (`page.tsx:130,137`) |
| F6 | web fact category pill untranslated | **RESOLVED** | `fact-item.tsx:25-27` `t(\`profile.facts.${category}\`).toUpperCase()` — normalized + translated, matches mobile |
| F7 | mobile ErrorBoundary + web (chat) error.tsx | **RESOLVED** | `apps/mobile/components/ui/app-error-boundary.tsx` exists; `_layout.tsx:468` exports `ErrorBoundary`→`AppErrorScreen`; `apps/web/app/(chat)/error.tsx` now present |
| F8 | mobile calendar-sync thin empty/loading | **RESOLVED** | loading `calendar-sync.tsx:574-585` `ActivityIndicator` spinner + text; empty `:643-653` `CalendarDays` glyph; offline `:615-639` `WifiOff` + retry; not-connected `:587-601` `LinkIcon` circle |
| F9 | mobile widget features list missing | **RESOLVED** | `advanced.tsx:32` imports `WIDGET_FEATURES`; `:518-525` renders featuresTitle + maps the 4 items |
| F10 | mobile delete-account borrowed copy | **RESOLVED** | `profile.tsx:934-936` now `t('profile.deleteAccount.offlineTitle')` + `offlineDescription` (dedicated keys) |
| F11 | web achievements lock glyph missing | **RESOLVED** | `achievements/_components/achievements-locked-state.tsx:12-18` Lock glyph circle, matches mobile |
| F12 | mobile data-export getErrorMessage leak | **RESOLVED** | `profile.tsx:345` now fixed `t('dataExport.error')`, aligned with web |
| F13 | mobile create-key no capabilities gate | **RESOLVED** | `advanced.tsx:125-128` `canCreateScopedKey` gates on `!capabilitiesQuery.isLoading && !capabilitiesQuery.error`; chip `disabled={!canCreateScopedKey}` (354); capabilities-error display (269) |
| F14 | revoke confirm surface divergent | **STILL OPEN** | web inline status-bad banner `advanced/page.tsx:448-481`; mobile kit `ConfirmDialog` `advanced.tsx:533-545` — not unified |
| F15 | borrowed offline copy (7 mobile surfaces) | **MOSTLY RESOLVED — 1 site left** | 6/7 + all inline setters migrated to `offline.*`/`chat.offline.*`/dedicated keys; **login.tsx:169 still borrows `calendarSync.notConnected`** |
| F16 | inverted instanceof Error exposes raw bulk-create | **RESOLVED** | web `breakdown-suggestion.tsx:108-110` + mobile `:345-347` now gate on `process.env.NODE_ENV === 'development' && err instanceof Error` — prod always shows `t('errors.bulkCreateHabits')` |

**13 of 16 RESOLVED.** Remaining: F1 (MED), F14 (LOW), F15 (MED, narrowed 7→1 site).

## Findings (STILL OPEN)

- **F1 · MED** · web `apps/web/components/habits/habit-list/drill-content.tsx` (no `drillError` branch) + mobile `apps/mobile/components/habit-list.tsx` (renders it) · check 3 error-state · A failed sub-habit fetch renders the "No sub-habits yet + Add" empty prompt on web (empty-as-error, lines 71-94); mobile shows the error text. `HabitListDrillContent` takes no `drillError` prop; the hook `use-drill-navigation.ts:67,92` sets `drillError` but the web panel never consumes it (the prop isn't threaded through `habit-list.tsx:1120` either). · Fix: thread `drill.drillError` into `HabitListDrillContent` and render an error+retry branch before the empty fallback, mirroring mobile.

- **F15 · MED** · mobile `apps/mobile/app/login.tsx:169-170` (consumed at `:461-462`) · check 3/5 copy quality · The login screen's `OfflineUnavailableState` still borrows `t('calendarSync.notConnected')` ("Not Connected" — a calendar-connection status label) as its offline TITLE and concatenates `auth.sendCode / auth.verify / auth.signInWithGoogle` button labels as its DESCRIPTION. This is the last surviving instance of the F10/F15 anti-pattern; the other 6 surfaces (profile delete-account, chat, support, retrospective ×2, upgrade ×2) and every inline `setError`/`setPortalError` setter were migrated to `t('offline.title')`/`t('offline.description')` / `t('chat.offline.*')` / dedicated `deleteAccount.offline*` keys, and the codifying test `intentional-offline-ux.test.tsx:467,476` was updated to assert `offline.title`. · Fix: replace `login.tsx:169-170` with `t('offline.title')` + `t('offline.description')` (the generic keys already exist in both locales). The auth screen is the most user-facing offline surface — highest residual stakes of the three.

- **F14 · LOW** · web `apps/web/app/(app)/advanced/page.tsx:448-481` (inline status-bad banner) + mobile `apps/mobile/app/advanced.tsx:533-545` (kit `ConfirmDialog`) · check 5 action-surface · API-key revoke confirms inline (status-bad 8% banner + inset ring + inline Cancel/Revoke) on web vs the kit `ConfirmDialog` modal on mobile. Same operation, divergent confirmation affordance across platforms. · Fix: pick one (kit ConfirmDialog) for both, per round-2.

## Checks with ZERO new findings

- **Check 4 (i18n microcopy quality)**: en.json + pt-BR.json — **zero em dashes** (U+2014, grep across `packages/shared/src/i18n`), zero banned SaaS filler. The central merge added `chat.offline.*`, `errors.offline`, `errors.bulkCreateHabits`, `profile.deleteAccount.offline*`, `offline.title/description`, and `habits.breakdown.frequencyQuantityLabel`/`removeHabit` to BOTH locales. CLEAN.
- **Check 6 (navigation consistency)**: unchanged — shared `PROFILE_NAV_ITEMS`, identical FAB rules, byte-equivalent confirm cluster. No findings.
- **Check 7 (first-run/activation)**: unchanged — symmetric onboarding gating + flag writes. No findings.

## Resolved since round 2 (do not re-report) — 13 findings

Error states: F2 (dev-gated profile error), F3 (friendly create-key key), F4 (web fetchApiKeys throws), F5 (facts fetcher throws + error/retry UI both platforms), F12 (data-export fixed key), F16 (dev-gated bulk-create ternary both platforms). Error boundaries: F7 (mobile `app-error-boundary.tsx` + `_layout.tsx` export; web `(chat)/error.tsx`). Empty/loading: F8 (calendar-sync spinner + 4 glyphed states), F11 (web achievements Lock glyph). Action surfaces: F9 (mobile WIDGET_FEATURES rendered), F13 (mobile capabilities gating + error display). Copy: F6 (translated category pill), F10 (delete-account dedicated offline keys — subsumed into the F15 cleanup).

## Verdict

**3 findings STILL OPEN: 0 HIGH · 2 MED · 1 LOW** (F1 web drill error unrendered; F15 login.tsx borrowed offline copy — 1 of the original 7 sites; F14 revoke surface divergent).

13 of 16 round-2 findings RESOLVED. The bulk of the UX-states batch landed: every raw-`error.message` leak is dev-gated or replaced with a friendly key, the mobile root `ErrorBoundary` + web `(chat)/error.tsx` exist, calendar-sync gained a spinner and four properly-glyphed states, the widget feature list and achievements Lock glyph reached parity, capabilities gating mirrors web, and the inverted bulk-create ternary is fixed on both platforms. The systemic F15 borrowed-offline-copy smell was cleared from 6 of 7 surfaces plus all inline setters (with the codifying test updated) — only `login.tsx` remains. Check 4/6/7: ZERO FINDINGS. Visual confirmation of states remains user-owned (DEF-1); all 3 open items are static-code evidence with both platform files named.
