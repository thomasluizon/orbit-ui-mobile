# Sweep 9 — test coverage & quality (ROUND 3, verification)

Read-only verification pass. Method: re-checked each of round-2's 15 still-open findings against the CURRENT trees by reading the actual test bodies (not filenames/grep substrings) per the round-3 calibration, and re-ran the dead-export discipline (count `__tests__` importers; a hook delegating to a tested shared core is NOT an uncovered logic gap). Suites not run (declared green). Baselines: ui-mobile 6399d00, api dec5bcc.

## Round-2 findings verified RESOLVED (do not re-report)

- **HIGH `apps/mobile/lib/google-auth.ts`** — `apps/mobile/__tests__/lib/google-auth.test.ts` now exists (6 tests). RESOLVED.
- **HIGH BFF auth routes / token-stripping boundary** — `apps/web/__tests__/app/api/auth-{google,send-code,verify-code}-route.test.ts` now exist (3+4+4 tests). The exact cookie boundary round-2 called out is asserted: `auth-google-route` + `auth-verify-code-route` both assert `setSessionCookies('jwt-token','refresh-token')` called AND `json).not.toHaveProperty('token')`/`refreshToken`, plus the failure path (`setSessionCookies` not called). RESOLVED (residual: `session`/`logout` routes + `lib/auth-proxy.ts` pure helpers — see still-open LOW).
- **HIGH `use-dismiss-guard` orphaned core** — REWIRED. `apps/mobile/hooks/use-dismiss-guard.ts` + `apps/web/hooks/use-dismiss-guard.ts` now both `import { resolveDismissGuardAction } from '@orbit/shared/hooks'` and delegate all three actions (request/confirm/cancel) to it; `packages/shared/src/__tests__/dismiss-guard-core.test.ts` still covers the logic. The tested core is no longer dead. RESOLVED.
- **MED `use-tag-selection` orphaned core** — REWIRED. Both platform hooks now consume the shared core (`createInitialTagSelectionState`/`toggleTagSelection`/`createAndSelectTagFlow` have production importers in `apps/`); the 319-line core test now reaches shipping code. RESOLVED.
- **MED mobile account-deletion** — `apps/mobile/__tests__/screens/account-deletion.test.tsx` now exists. RESOLVED.
- **MED `use-login-code-entry`** — `apps/mobile/__tests__/hooks/use-login-code-entry.test.ts` now exists. RESOLVED.
- **MED `use-chat-reward`** — `apps/mobile/__tests__/hooks/use-chat-reward.test.tsx` now exists (3 tests): asserts the cached-profile patch `aiMessagesLimit).toBe(10)` after a rewarded ad, the not-earned no-patch branch, and the gate. The monetization cache mutation is covered. RESOLVED.
- **MED mobile mirrors (highest-priority subset)** — `apps/mobile/__tests__/hooks/use-habit-queries.test.ts`, `use-goal-queries.test.ts`, `use-calendar-data.test.ts` now exist (round-2's named priorities, incl. the mobile-only `use-habit-queries` refetchInterval gate). RESOLVED for the called-out three.
- **MED backend D3 error-payload shape** — `tests/Orbit.Infrastructure.Tests/Extensions/ResultActionResultExtensionsTests.cs` now has `ToErrorResult_Failure_EmitsErrorAndErrorCode` asserting `objectResult.Value.Should().BeEquivalentTo(new { error, errorCode })` for a normal failure AND a pay-gate failure, PLUS `ToErrorBody_AppError_EmitsErrorAndErrorCode` covering the previously-zero-reference `ToErrorBody(this AppError)` path. RESOLVED.

## STILL-OPEN findings

- LOW · `apps/web/app/api/auth/session/route.ts` + `apps/web/app/api/auth/logout/route.ts` (no route test) AND `apps/web/lib/auth-proxy.ts` (8 exported pure helpers: `isRecord`, `extractErrorMessage`, `resolveRequestId`, `buildRequestIdResponseHeaders`, `resolveResponseRequestId`, `buildAuthErrorPayload`, `logAuthRouteFailure`, `buildEmailLogContext`) · The session-establishing routes (google/verify-code/send-code) are now tested, but `session` (reads cookie → returns user) and `logout` (clears cookies) have no test, and `auth-proxy`'s helpers are exercised only transitively through the route tests, never unit-tested · Severity LOW not HIGH: both routes are ~22–27 lines of thin cookie I/O with no token-stripping logic (the round-2 HIGH boundary lives in the now-tested routes); the helpers are pure and small · Fix: a thin `auth-proxy.test.ts` over the pure helpers (round-2's own suggestion) + optional session/logout smoke tests (cookie set on session, cleared on logout).

- LOW · `apps/mobile/hooks/use-review-reminder.ts` (useReviewReminder gating matrix + StoreReview→Linking fallback) · Round-2 MED NOT filled — still only `stores/review-reminder-store.test.ts` (store); no hook test exercises the gating (onboarding done, completionCount ≥ 20, activeDays ≥ 5, `dismissedUntil >= today` string compare, foreground, celebration suppression) or the StoreReview→Play-URL fallback chain · Severity downgraded MED→LOW: pure gating over already-tested store state; no cache/network side-effects · Fix: renderHook matrix over store states + mocked `expo-store-review`/`Linking`.

- LOW · mobile mirrors still missing direct tests: `apps/mobile/hooks/use-summary.ts`, `use-habit-form.ts`, `use-resolve-clarification.ts` · Round-2 MED partially filled (the 3 priority mirrors landed; these 3 did not) · Severity MED→LOW per calibration: each is a thin platform wrapper over shared logic that IS tested in `packages/shared` — `use-summary`→`@orbit/shared/query` keys, `use-habit-form`→`@orbit/shared/validation`, `use-resolve-clarification`→`@orbit/shared/api`. The mobile-specific seam left uncovered is `use-resolve-clarification`'s success-gated invalidation set · Fix: thin renderHook ports of the web suites; prioritize `use-resolve-clarification` invalidation.

- LOW · `orbit-api tests/Orbit.Application.Tests/Common/ErrorCodesAndMessagesTests.cs` (code↔message coherence) · Round-2 LOW NOT filled — the test was migrated to read the new `AppError`-based catalog (`((AppError)field.GetValue(null)!).Message`) but still asserts only format/non-empty/no-duplicate; there is NO test iterating `ErrorMessages` `AppError` fields asserting each `.Code` exists in `ErrorCodes`. A wrong `Code` baked into an `AppError` remains uncaught · Fix: add the iteration test (every `AppError.Code` ∈ `ErrorCodes`; optionally every `ErrorCodes` value referenced by some `AppError`).

- LOW · `apps/web/__tests__` class-name / CSS-selector assertions (~10 files) · Round-2 LOW NOT filled — still assert Tailwind classes/selectors vs behavior: `pages/login.test.tsx` (`toHaveClass('max-w-[26rem]')`), `pages/streak.test.tsx` (`querySelector('.streak-hero__count')`), `components/goals/goal-card.test.tsx` + `habits/habit-checklist.test.tsx` (`className.toContain('line-through')`), `goals/goal-list.test.tsx` (`drag-chosen`/`drag-ghost`), `ui/skeleton.test.tsx`, `chat/message-bubble.test.tsx`, `ui/markdown.test.tsx`, `ui/app-overlay.test.tsx`, `onboarding/onboarding-flow.test.tsx`. Violates apps/web/CLAUDE.md "assert behavior + data attributes, NOT class names" · Fix: role/test-id/data-attribute queries where the visual state is load-bearing.

- LOW · `orbit-api` reflection / private-member tests (unchanged) · Six AI-service test files still invoke private statics via `BindingFlags.NonPublic` (`BuildExtractionPrompt`, `BuildHistoryTranscript`, `NormalizeSchemaTypes`, `ConvertToSdkTool`, `BuildRetrospectivePrompt`, `BuildSummaryPrompt`, `SelectScheduledHabits`, `GenerateFallback`); `GetHabitByIdQueryHandlerTests` writes private `Habit._children`. Renaming a private breaks tests with no behavior change · Fix: assert at the mocked AI-client boundary; build the graph via public `Habit` API.

- LOW · hand-rolled `NormalizedHabit` fixtures (unchanged) · 12+ files still duplicate ~35-field habit literals instead of `packages/shared/src/__tests__/factories.ts#createMockHabit` (e.g. `apps/web/__tests__/hooks/use-habit-visibility.test.ts`, `apps/mobile/__tests__/hooks/use-habits.test.ts`, `packages/shared/src/__tests__/habit-visibility.test.ts`) · Fix: replace local `makeHabit` helpers with `createMockHabit(overrides)`.

- LOW · thin wrappers only-ever-mocked (unchanged) · `apps/mobile/lib/offline-runtime.ts` (4-case connectivity resolution), `apps/mobile/hooks/use-horizontal-swipe.ts` (threshold/direction math), `apps/web/hooks/use-date-format.ts` + mobile twin, `apps/web/hooks/use-calendar-events.ts` — never exercised directly · Fix: small unit tests where cheap (offline 4-case; swipe threshold/direction); accept date/calendar wrappers as low risk.

- LOW · `apps/mobile/components/ui/create-api-key-modal.tsx` (unchanged) · Mobile modal still does not import the shared `MAX_API_KEY_NAME_LENGTH`/`parseApiKeyExpiryUtc` validators (web modal does + is tested); no mobile test on name-length/expiry. `MAX_API_KEY_NAME_LENGTH` has no direct value assertion in `packages/shared/src/__tests__` (only transitive via the web modal) · Fix: once mobile adopts the shared validators, mirror the web modal test; add a one-line constant assertion.

## Fresh-coverage scan (new wave-2/3 logic)

No new wave-2/3 production logic with zero coverage beyond the residual hooks above. The 17 mobile hooks with no own `__tests__/hooks/*.test.*` file were each checked for shared-core delegation: `use-dismiss-guard`/`use-tag-selection` (rewired+tested cores), `use-drill-navigation`/`use-habit-visibility`/`use-retrospective`/`use-bulk-actions` (tested `@orbit/shared/utils`), and the tour-mock/scroll/toast/speech hooks (platform-glue, no shared logic) — none represents uncovered shared logic. Per calibration, the 21 `types/*` schemas are imported + `.safeParse`-tested in `__tests__/types.test.ts` — NOT dead.

## Cross-cutting (NOT a coverage finding)

- D3 client-half remains additive/unfinished (matches round-2): backend emits stable `errorCode`s and the client added `extractBackendErrorCode` + `getFriendlyErrorKey` (both tested), but `getErrorMessage` still returns the raw backend string and `CONTEXTUAL_RULES` still match English fragments; no `error-codes.json` catalog. Non-breaking improvement, not an uncovered symbol — recorded for the fix-pass disposition only.

## Verdict

HIGH 0 · MED 0 · LOW 9 — all 9 are pre-existing round-2 residuals (5 carried verbatim, 3 downgraded MED→LOW because the underlying logic is tested in a shared core / the route has no token logic, 1 backend coherence test). All 3 round-2 HIGH (google-auth, BFF-auth boundary, dismiss-guard orphan), all 4 of the targeted round-2 MED frontend gaps (tag-selection orphan, account-deletion, login-code-entry, chat-reward), the 3 priority mobile mirrors, and the backend D3 error-payload shape are RESOLVED. Dominant round-2 theme (orphaned cores masking untested shipping hooks) is fully closed — both cores rewired.
