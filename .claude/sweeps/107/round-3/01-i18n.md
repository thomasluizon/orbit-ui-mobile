# Round-3 sweep #1 — i18n parity + hardcoded strings (issue #107) — VERIFICATION

Baseline: ui-mobile `6399d00` (committed green), orbit-api `dec5bcc` on `chore/107-code-health-sweep`. Read-only. Verifies every round-2 `01-i18n.md` finding against current code.

Tooling (left in `round-3/`): `check-refs.mjs` (static `t()`/`i18n.t()` resolver over 524 source files, test files excluded) + `check-orphans.mjs` (conservative orphan detector with dynamic-builder tail-segment gating). Both re-runnable.

## Domain 1 — Key parity (en ↔ pt-BR)

VERIFIED PERFECT. Node leaf-diff: en = **1625**, pt = **1625**, 0 missing either direction, 0 structural mismatch. Round-2 was 1609=1609; the +16 is the W2.5 central merge (`chat.offline.*`, `errors.offline`, `habits.breakdown.*`, `orbitMcp.invalidExpiry`, `auth.minutesShort`, `updatePrompt.version`, `notifications.channel.default`, etc.) landing in BOTH locales in the same diff (`9bdcdff`). **No parity findings.**

## Domain 2 — Referenced-but-missing keys

`check-refs.mjs`: **NO missing static `t()` keys** across apps/web + apps/mobile + packages/shared. Every round-2 missing key is resolved:
- `upgrade.comparison.subHabits.tooltip` — **FIXED.** Both platforms repointed to the existing `upgrade.features.subHabits.tooltip` (web `create-habit-modal.tsx:331`, mobile `sub-habit-editor.tsx:62`). The phantom `upgrade.comparison` namespace was correctly NOT minted (D34 honored).
- `orbitMcp.invalidExpiry` — **FIXED** (present both locales; consumed by web + mobile api-key modals).
- `auth.minutesShort` (`{minutes}`) — **FIXED** (present both locales).
- `updatePrompt.version` (`{previous,next}`) — **FIXED** (present both locales).

The 5 merged keys confirmed present in both locales with real copy: `chat.offline.title`, `chat.offline.description`, `errors.offline`, `habits.breakdown.frequencyQuantityLabel`, `habits.breakdown.removeHabit`. The round-2 "chat.offline lost edit" artifact is resolved — both keys exist AND the chat agent renders them (no longer referenced-but-missing nor orphaned).

**No referenced-but-missing findings.**

## Domain 3 — Orphan keys

`check-orphans.mjs` surfaced 11 candidates, ALL confirmed dynamic-builder-reachable (non-findings, identical to round-2's gated set):
- `habits.frequency.every{Day,Week,Year,NDays,NMonths,NYears}` — frequency-label dynamic constructor.
- `preferences.color{Purple,Green,Rose,Orange,Cyan}` — built via `` t(`preferences.color${capitalize(option.value)}`) `` at `apps/mobile/app/preferences.tsx:232,424` over the closed `colorSchemeOptions` set.

**No orphan findings.**

## Domain 4 — Hardcoded user-facing strings (apps/web + apps/mobile)

Round-2's 6 carried-forward items re-verified by direct read:
- ai-settings raw enum (`category.toUpperCase()`) — **FIXED.** Moved to `apps/web/app/(app)/ai-settings/_components/fact-item.tsx:27`: `` t(`profile.facts.${category}`).toUpperCase() `` — localized, parity with mobile `ai-settings.tsx`.
- "Google Calendar" hardcoded JSX (web + mobile) — **FIXED.** Both now `t('calendar.title')` (web `calendar-sync/page.tsx:172`, mobile `calendar-sync.tsx:499`).
- Mobile push channel `name: 'Default'` — **FIXED.** Now `i18n.t('notifications.channel.default')` (`use-push-notifications.ts:180`; key present both locales).
- Mobile push hook English display-state strings (`Push token is unavailable` / `Failed to sync push…`) — **FIXED/GONE.** Grep finds zero matches in `use-push-notifications.ts`.

One round-2 LOW remains, plus one re-classified down:

- **LOW · apps/web/components/goals/goal-list.tsx:169 · apps/web i18n rule (web-only by platform nature)** — `aria-roledescription="draggable item"` still hardcoded English; announced by screen readers regardless of locale. Mobile reorder uses native gesture handlers (no aria attribute) so there is no mobile mirror. Fix: route through `t()`; suggested key `goals.dragItem` en `"draggable item"` / pt `"item arrastável"` (key currently absent from both locales — add in the same diff). Lowest-impact i18n gap; the visible drag label `aria-label={goal.title}` is already dynamic.
- **LOW · apps/web/hooks/use-push-notification-preferences.ts:146 · rule 2 (dead literal, NOT user-facing)** — `throw new Error('Failed to subscribe to push notifications')` is caught by a bare `catch {}` at line 217 that discards the error and sets `status: 'sync-failed'` (UI renders a localized status string, never `error.message`). So this is NOT a localization gap (provably never rendered) — it is a dead English literal. Fix: `throw new Error()` (drop the message) or omit the string. Re-classified from round-2's i18n-LOW to a rule-2 dead-copy LOW because the render path is now confirmed to discard it.

**No NEW hardcoded strings** were found in either app beyond these two.

## Domain 5 — Backend tail (orbit-api errorCode emission)

Audited `dec5bcc`. **The HIGH controller-bypass findings are RESOLVED.** Grep for `new { error = result.Error }` across `src/Orbit.Api/Controllers/`: **ZERO matches** (round-2 had ~37+). Every controller failure now routes through `ToErrorResult()` / `ToPayGateAwareResult()` / `.ToErrorBody()`, which emit `{ error, errorCode }`. Spot-verified:
- **AuthController** — round-2's 7 bypass sites gone; failures ship codes.
- **SubscriptionController.cs:71** — the single remaining `new { error = … }` deliberately includes `errorCode = result.ErrorCode` (a `NotFound` for `NoActiveSubscription`). Correct, not a bypass.
- **AiController.cs:324** — the round-2 raw `firstError.ErrorMessage` is now wrapped `new AppError(firstError.ErrorCode ?? ErrorCodes.ValidationError, firstError.ErrorMessage).ToErrorBody()` — code-carrying, well-formed.
- Round-2 MED inline bare-English payloads (ChatController "Chat history too large"/"Invalid chat history format"/"Invalid client context format", AiController "Pending operation not found", CalendarController "Enabled is required.", SyncController "No mutations provided."/"Maximum 100 mutations") — grep finds **ZERO** of these literals remaining in Controllers.
- **OAuthController** — 9 `new { error = "…" }` remain (`unsupported_response_type`, `unsupported_grant_type`, `invalid_grant`+`error_description`, `server_error`, `PKCE with S256 is required`, redirect-uri error at lines 120,123,126,158,185,249,252,256,272). These are OAuth-2 spec machine codes / `error_description` on the redirect-driven authorize+token flow, NOT localized user-facing sentences. Correctly exempt (round-1/round-2 classification holds). **Not a finding.**

Client side: `getFriendlyErrorKey` (error-utils.ts) consumes `errorCode`; with controllers now emitting codes, the localized-error catalog is unblocked end-to-end. **No backend i18n findings.**

(Validator `.WithMessage` prose — the round-2 MED about ~64 FluentValidation bare-string sites — was a backend-internal robustness item under the "validation codes" umbrella, not a frontend i18n parity gap, and FE-side `CONTEXTUAL_RULES` substring-matching still works; it is out of this FE-i18n sweep's actionable scope and not re-counted here.)

## Verdict

**2 LOW** (1 web-only aria string; 1 dead non-rendered literal). Parity: ZERO. Orphans: ZERO. Referenced-but-missing: ZERO. Backend errorCode emission: RESOLVED.

Net vs round-2 (was 3 HIGH · 7 MED · 7 LOW = 17): **15 of 17 resolved.** The 2 survivors are both LOW and both web-only-by-nature / latent (no cross-platform divergence, no user-visible English on a localized surface).
