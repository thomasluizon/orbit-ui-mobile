# Round-7 convergence verify — sweep #4 accessibility (READ-ONLY)

Target: HEAD `d920589` (committed green). No source edits. All claims re-derived with own node scripts:
- `.claude/sweeps/107/round-7-verify/verify-contrast.mjs` — independent WCAG relative-luminance recompute (text-on-solid, text-on-tint, non-text, fg-on-fill).
- `.claude/sweeps/107/round-7-verify/grep-status-usage.mjs` — every `--status-overdue`/`--status-bad`/`-text`/`tokens.status*` + raw status-hex usage (297 token + 26 raw), classified.
- `.claude/sweeps/107/round-7-verify/base-as-text.mjs` — isolates BASE-tone (non-`-text`) tokens by render role.
- `.claude/sweeps/107/round-7-a11y-scan.mjs` (existing) re-run — mobile role scan.
- `.claude/sweeps/107/round-7-verify/scan-names.mjs` — mobile accessible-NAME complement scan.

Token values verified identical at source: web `globals.css:194-243` ≡ shared `neutral-ramp.ts:213-226`. Light overdue `#e17100` / overdueText `#b45b00` / bad `#e7000b` / badText `#e7000b`; dark all `#fe9a00` / `#fb2c36`.

---

## Claim 1 — zero amber/overdue-base-as-text on SOLID surfaces (grep 3→0)

`base-as-text.mjs` + focused rg enumerate EVERY `--status-overdue`/`tokens.statusOverdue` BASE (negative-lookahead on `-text`/`Text`) used as a real TEXT color. Complete set (5 sites) and surface classification:

| Site | surface | in DEF-R7? |
|---|---|---|
| web `conflict-warning.tsx:23` (MEDIUM) | `bg-[var(--status-overdue)]/10` → overdue@10% **tint** | yes (line 140) |
| web `trial-banner.tsx:47` "lastDay" | parent bg overdue@10% **tint** (`:30`) | yes (line 144) |
| web `upgrade/page.tsx:701/708` trial block | parent bg overdue@10% **tint** (`:691`) | yes (line 145) |
| web `goal-card.tsx:41` | `color` = progress-**ring fill** (NON-text); the `textColor` field = `--status-overdue-text` | n/a (non-text) |
| mobile `trial-banner.tsx:130` `urgentText` | parent bg overdue@10% **tint** (`:53`) | yes (line 144) |

**Zero overdue-base-as-text on a SOLID surface.** The 2 solid-surface amber-as-text sites that existed at round-6 (web `upgrade/page.tsx:95` + mobile `upgrade.tsx:147` UsageCard/UsageStats value on the card) were migrated to `--status-overdue-text`/`tokens.statusOverdueText` in `d920589` (confirmed in the commit diff). `verify-contrast.mjs` §A: overdueText on canvas 4.52 / white card 4.73 / dark 8.56 — all PASS AA. The 3→0 grep claim holds.

bad-base-as-text on SOLID surfaces (~20 web + ~24 mobile error/destructive copy, plus the goal-card overdue-deadline badge web `:179`/`:58`, mobile `:204`/`:99` which sits on the solid card — badge has only a hairline inset, no tint fill: web `goal-card.tsx:20-27`, mobile `badge` style `:284-290`) all PASS: §A badText canvas 4.56 / white card 4.77 / dark 5.29. Correctly NOT findings (matches brief: "bad-text-on-solid passes AA, is fine").

## Claim 2 — zero role-less interactive mobile elements (scanner 30→0)

`round-7-a11y-scan.mjs` re-run: **TOTAL interactive tags 238, FLAGGED 0.** Accessible-NAME complement (`scan-names.mjs`, 234 onPress elements): 1 raw flag = `ai-settings.tsx:516`, a **false positive** — that `Pressable` is `accessibilityRole={selectMode ? 'checkbox' : 'none'}` with `onPress` only in select-mode; its name comes from the fact-text descendants (beyond the 600-char heuristic window), `accessibilityState={{checked}}` wired. No genuine missing-role or missing-name interactive element. The 30→0 claim holds.

## DEF-R7-A11Y-STATUS-TINT family — every tint site re-derived, all covered

`verify-contrast.mjs` §B reproduces the deferral table exactly (light FAIL / dark PASS):

| Surface (tint over canvas) | light | dark | family entry |
|---|---|---|---|
| denialCard bad@8% — web mb `:233,236`/mobile `:362,370,376` | 3.96 ✗ | 5.07 ✓ | line 139 |
| conflict HIGH / Failed chip bad@10% — web cw `:18`, ac `:78-79`/mobile cw `:22-23`, ac `:182-183` | 3.82 ✗ | 4.99 ✓ | lines 140-141 |
| pastDue badge bad@18% — web upgrade `:417-418`/mobile `:655-658` | 3.30 ✗ | 4.61 ✓ | line 146 |
| trial/expiry/upgrade overdue@10% — listed sites | 4.06 ✗ | 8.44 ✓ | lines 143-145 |
| amber `<Badge>` overdue@18% — web badge `:23-24`/mobile `:56-57` | 3.72 ✗ | 6.29-7.29 ✓ | line 142 |

Every text-on-same-tone-tint site found by `grep-status-usage.mjs` maps to a DEF-R7 family entry. No tint text site is uncovered. Non-color cue (icon + literal status wording) present at each → WCAG 1.4.1 satisfied; only 1.4.3 short, light only. Mobile twins for denialCard, conflict-warning, action-chips, badge, trial-banner, expiry-warning, pastDue all confirmed present and in the family (cross-platform parity holds).

Raw-hex grep (26): all are `--primary` scheme values (`#ff2056`/`#ec003f`/`#ff6900`/`#f54900`), the tag literal `#dc2626`, or the status-token definitions themselves. **Zero stray status hexes used as text-on-solid.**

## Other a11y categories — spot-checked clean

- **fg-4-as-text:** 5 hits, all ICONS (lucide chevrons w/ width+height+color, or the select caret `text-[var(--fg-4)]`). fg-4 at 3:1 icon floor — none body text. Clean.
- **outline:none w/o replacement:** 16 web files; every site pairs `focus:outline-none` with `focus:shadow-[inset_0_0_0_2px_var(--primary)]` (app-select `:29`, checklist-templates `:132`, edit-goal-modal `:376`, …) or relies on the global `:focus-visible` rule (`globals.css:937`); `create-habit-modal.tsx:295` text-input uses the global ring. Clean.
- **heading hierarchy:** web h1×8 / h2×9 / h3×8 / h4×1 — descending, no h5/h6, no skip. Clean.
- **reduced-motion gating:** both platforms — web `prefers-reduced-motion` (globals.css + overlay/dialog/popover/status-dot/page), mobile `isReduceMotionEnabled` via `lib/motion.ts` + consumers (message-bubble, status-dot, habit-row, skeleton, progress-bar). Wired.
- **focus rings:** global `:focus-visible` indicator at `globals.css:937`. Present.
- **modal focus restore:** `confirm-dialog.tsx` stores `previouslyFocusedElement` `:67` + restores `:108`; `app-overlay.tsx` store `:139` + restore; `popover.tsx` restores to trigger `:108`. Focus trap + restore present in all three overlay primitives.

---

## Findings

- LOW · apps/web/components/chat/conflict-warning.tsx:23 (+ trial-banner.tsx:47,75; upgrade/page.tsx:701,708) · DEF-R7 justification imprecise for the WEB amber-on-tint sites · These web sites use the BASE `--status-overdue` (light 2.75 on overdue@10% tint), whereas their mobile twins use `tokens.statusOverdueText` (4.06). The family entry's "migrating to the `-text` token is a light no-op" is true only for the bad tone and the mobile amber twins; for these web amber sites a base→`-text` swap would IMPROVE light 2.75→4.06 (still <4.5, so still a legitimate ceiling deferral). Also a minor web/mobile parity inconsistency (web base vs mobile `-text` for the identical surface). Does NOT affect convergence — every value still fails 4.5 light / passes dark and remains correctly deferred. Fix (optional, when the tint family is revisited): align web to `--status-overdue-text` for parity with mobile.

## Verdict

Both convergence claims VERIFIED.
- amber/overdue-base-as-text on SOLID surfaces: **0** (independently grep-proven; the 5 residual overdue-base-as-text sites are all on overdue tint or a ring fill, all in DEF-R7).
- role-less interactive mobile elements: **0** (round-7 scanner 238/0; accessible-name complement 0 genuine).
- Every status-tint residual is covered by DEF-R7-A11Y-STATUS-TINT (which subsumes DEF-R4-A11Y-3 + DEF-R6-A11Y-1); web habit-row APG nesting covered by DEF-R6-A11Y-2. No uncovered residual.

One LOW documentation/parity nit (above); not a blocker. Otherwise the registered deferrals cover every residual.
