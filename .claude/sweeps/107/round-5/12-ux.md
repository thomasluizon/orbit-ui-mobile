# Round-5 sweep #12 — UX states + copy (issue #107) — FINAL VERIFICATION

Baseline: ui-mobile `3520d10` (committed green). Read-only. Verifies the three round-3 STILL-OPEN items (F1 drill error, F15 login offline, F14 revoke surface) landed in round 4, plus error-state / ErrorBoundary / offline-copy integrity.

## Round-3 STILL-OPEN — all RESOLVED in round 4

- **F1 (web drill error unrendered) — FIXED.** `apps/web/components/habits/habit-list/drill-content.tsx:42-57` now renders a `role="alert"` error branch (`color: var(--status-bad)`, padded centered) BEFORE the "No sub-habits yet + Add" empty fallback; `apps/web/components/habits/habit-list.tsx:1123` threads `drillError={drill.drillError}` into the panel. Mobile already rendered it (`apps/mobile/components/habit-list.tsx:1640-1641`). A failed sub-habit fetch now shows an error on BOTH platforms instead of web's empty-as-error. The hook→consumer wiring round-3 flagged missing is now complete.

- **F15 (login borrowed offline copy) — FIXED.** `apps/mobile/app/login.tsx:170-171` `t('offline.title')` + `t('offline.description')`, rendered by `OfflineUnavailableState` (`:461-465`). The last `calendarSync.notConnected` borrow is gone. **Global offline-copy audit:** `calendarSync.notConnected` now appears ONLY in `apps/mobile/app/calendar-sync.tsx` (`:285,302,545,549`) — its intentional calendar-connection-status home. ZERO borrowing on any other surface (profile, chat, support, retrospective, upgrade, login all on generic/dedicated keys). Exactly the intended end-state.

- **F14 (revoke surface divergent) — FIXED.** Web `apps/web/app/(app)/advanced/page.tsx` reduced the revoke chip (`:444-453`) to `setRevokingKeyId(key.id)`; confirmation moved to the kit `<ConfirmDialog>` (`:639-650`). Mobile `apps/mobile/app/advanced.tsx:533-543` uses the same `ConfirmDialog` + identical `orbitMcp.revoke` / `orbitMcp.revokeConfirm` keys. The inline-banner-vs-modal split is unified on `ConfirmDialog`.

## Error states / ErrorBoundary / copy — re-confirmed intact

- **ErrorBoundary** — mobile `apps/mobile/app/_layout.tsx:468` exports the Expo-Router `ErrorBoundary` wiring `apps/mobile/components/ui/app-error-boundary.tsx`; web has BOTH `apps/web/app/(app)/error.tsx` and `apps/web/app/(chat)/error.tsx`. Unchanged by the r4 chat split.
- **Error states** — the round-3-resolved set (dev-gated profile/bulk-create error messages, friendly create-key key, web fetcher `throw`-on-`!ok`, capabilities gating) is untouched; the new web drill error branch (F1) adds one more properly-glyphed error state.
- **Copy** — en/pt-BR remain em-dash-free; the r4 additions (`offline.*`, `goals.dragItem`) carry no banned filler.

## Verdict

**ZERO FINDINGS.** All three round-3 STILL-OPEN items resolved in round 4: F1 drill error now rendered on both platforms (hook→panel wired on web); F15 login offline on generic keys with `calendarSync.notConnected` confined to the intentional calendar surface only; F14 revoke unified on `ConfirmDialog`. ErrorBoundary (mobile + web app/chat) and the resolved error-state set intact through the chat split. Net: round-3's 2 MED + 1 LOW → 0.
