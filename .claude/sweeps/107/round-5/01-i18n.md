# Round-5 sweep #1 — i18n parity + referenced/orphan/hardcoded + backend errorCode (issue #107) — FINAL VERIFICATION

Baseline: ui-mobile `3520d10` (committed green). Read-only. Re-runs the round-3 tooling (`check-refs.mjs`, `check-orphans.mjs`) + an inline en/pt leaf-diff, and verifies the two round-3 LOW survivors landed in round 4.

## Domain 1 — Key parity (en ↔ pt-BR)

VERIFIED PERFECT. Inline leaf-diff: en = **1628**, pt = **1628**, 0 missing either direction. Brief expected "was 1628" — count held exactly (round-3 was 1625; the +3 are r4 additions `goals.dragItem`, `offline.title`, `offline.description`, all landed in BOTH locales). **ZERO parity findings.**

## Domain 2 — Referenced-but-missing keys

`check-refs.mjs`: **NO missing static `t()` keys** across apps/web + apps/mobile + packages/shared. Brief-named keys confirmed present in BOTH locales:
- `goals.dragItem` — en `"draggable item"` / pt `"item arrastável"` — **present** (the round-3 LOW). Consumed at `apps/web/components/goals/goal-list.tsx:171` `aria-roledescription={t('goals.dragItem')}` (was a hardcoded English literal in round 3). **FIXED.**
- `offline.title` — en `"You're offline"` / pt `"Você está offline"` — **present.**
- `offline.description` — en `"Check your connection and try again."` / pt `"Verifique sua conexão e tente novamente."` — **present.**

**ZERO referenced-but-missing findings.**

## Domain 3 — Orphan keys

`check-orphans.mjs` surfaced **11** candidates — the identical dynamic-builder-reachable set from round 3 (`habits.frequency.every{Day,Week,Year,NDays,NMonths,NYears}` built by the frequency-label constructor; `preferences.color{Purple,Green,Rose,Orange,Cyan}` built via `` t(`preferences.color${capitalize(...)}`) `` at `apps/mobile/app/preferences.tsx`). All non-findings. **ZERO orphan findings.**

## Domain 4 — Hardcoded user-facing strings

Both round-3 LOW survivors are RESOLVED in round 4:
- `goal-list.tsx:171` `aria-roledescription` — **FIXED** (now `t('goals.dragItem')`; key added to both locales — see Domain 2).
- `apps/web/hooks/use-push-notification-preferences.ts:146` dead non-rendered literal — **FIXED** (now `throw new Error()`, message dropped; the round-3 rule-2 dead-copy nit is closed).

No NEW hardcoded user-facing strings introduced by the r4 chat/calendar splits or the `highlightText→shared` extraction (the shared util returns structured `HighlightSegment[]`, no copy).

## Domain 5 — Backend errorCode emission

Out of round-5 FE scope (api repo `dec5bcc` already verified RESOLVED in round 3: zero `new { error = … }` controller bypasses; failures route through `ToErrorResult`/`ToErrorBody` emitting `{ error, errorCode }`; OAuth/Subscription exemptions classified). No FE-side regression: `getFriendlyErrorKey` (`error-utils.ts`) still consumes `errorCode`. No new backend i18n findings in this FE pass.

## Verdict

**ZERO FINDINGS.** Key parity 1628==1628 (0 drift). Referenced-but-missing: ZERO. Orphans: ZERO (11 dynamic-builder non-findings). Both round-3 LOW survivors (`goals.dragItem` aria string; dead push literal) landed in round 4. Net: round-3's 2 LOW → 0.
