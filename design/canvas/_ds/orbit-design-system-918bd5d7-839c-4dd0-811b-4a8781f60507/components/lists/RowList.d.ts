/** Shared-panel list container (settings-style rows). NOT for habits: each top-level habit sits on its own panel; a shared panel with internal hairlines on habits is two separation devices on one boundary. */
export interface RowListProps { children?: any; style?: any; }
export declare function RowList(props: RowListProps): any;
