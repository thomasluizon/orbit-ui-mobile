/** Static badge: chip radius 8, NEVER a pill (999 means interactive). Neutral only; a static badge is not an accent role. */
export interface BadgeProps { variant?: 'solid' | 'outline'; children?: any; }
export declare function Badge(props: BadgeProps): any;
