/** Quiet informational card. ONE tone only; no accent, no coloured side stripe, no severity variants. */
export interface InfoCardProps { icon?: any; children?: any; }
export declare function InfoCard(props: InfoCardProps): any;
