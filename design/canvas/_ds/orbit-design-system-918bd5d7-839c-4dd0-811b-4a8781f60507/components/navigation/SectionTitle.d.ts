/** Section heading (20/500) with optional mono uppercase eyebrow. Never add a subtitle that restates it. */
export interface SectionTitleProps { children?: any; eyebrow?: string; }
export declare function SectionTitle(props: SectionTitleProps): any;
