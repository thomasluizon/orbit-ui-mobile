// ---------------------------------------------------------------------------
// Cross-platform API contract constants
// Mirror values from orbit-api's AppConstants. Single source of truth for
// limits that the backend enforces and the frontend should honour to avoid
// wasted round-trips.
// ---------------------------------------------------------------------------

/** Mirror of AppConstants.MaxClarificationValueLength on the backend. */
export const MAX_CLARIFICATION_VALUE_LENGTH = 2048
