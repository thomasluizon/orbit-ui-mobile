/**
 * Mirror of orbit-api AiSummaryService.MaxSummaryChars: summaries at or under
 * this length render in full; anything longer gets the clamp + expand affordance.
 */
export const AI_SUMMARY_MAX_CHARS = 300
