/** True circle with a sweep over a track (deliberately not the logo's tilted-ellipse shape). Accent while unfinished; at 100% the sweep goes neutral. */
export interface ProgressRingProps {
  value?: number
  size?: number
  label?: string
}
