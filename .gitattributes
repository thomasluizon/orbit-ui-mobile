packages/shared/src/types/__generated__/*.generated.ts text eol=lf
.claude/workflows/*.mjs text eol=lf
.claude/skills/**/*.mjs text eol=lf
tools/*.sh text eol=lf
# Machine-parsed harness artifacts and node scripts check out as LF everywhere:
# core.autocrlf=true materialized the committed work orders with CRLF on a fresh
# Windows clone and drive-queue parsed ZERO of the 214 orders on disk. The
# parsers are CRLF-tolerant too (belt), this pins the checkout (suspenders).
# .claude/drive/ is gitignored, so it needs no rule.
.claude/workorders/*.md text eol=lf
CLAUDE.md text eol=lf
.claude/rules/*.md text eol=lf
# A skill file under a byte ceiling must measure the same on every platform: this
# worktree reads CRLF and a Linux CI checkout reads LF, a 772 byte difference on
# orchestrate/SKILL.md alone, so an unpinned ceiling is wrong on one of them by
# construction. Same reason CLAUDE.md and the rules files above are pinned.
.claude/skills/**/*.md text eol=lf
*.mjs text eol=lf
# arch-map artifacts are regenerated by CI and diffed byte-for-byte; a CRLF
# checkout would make the drift gate compare different bytes than the LF the
# generator writes. linguist-generated collapses them in the GitHub diff view
# so a reviewer reads the ~200 hand-written analyzer lines, not the ~22k-line
# regenerated churn that made the #722 Pullfrog review crawl (2026-08-14).
architecture.json text eol=lf linguist-generated=true
architecture.html text eol=lf linguist-generated=true
packages/shared/src/types/__generated__/*.generated.ts linguist-generated=true
