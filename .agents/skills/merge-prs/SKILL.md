---
name: merge-prs
description: Merge a frozen set of already-approved PRs after /orchestrate, with optional PR links or conversation-context discovery; admin-squash merges in dependency order, updates Linear, and cleans only target branches/worktrees.
---

# merge-prs

The canonical definition of this skill is `.claude/skills/merge-prs/SKILL.md`.

Read that file now and follow it exactly. Do not act on this pointer alone: it carries no behaviour,
deliberately. Both hosts run one definition so Claude and Codex cannot fork.
