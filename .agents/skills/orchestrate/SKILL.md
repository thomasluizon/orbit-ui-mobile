---
name: orchestrate
description: Codex compatibility adapter for Orbit's canonical /orchestrate workflow. Use when executing Linear tickets through reviewed pull requests.
---

# /orchestrate

Read `.claude/skills/orchestrate/SKILL.md` completely and execute it as the canonical
workflow. Pass the caller's arguments unchanged as `$ARGUMENTS`.

Do not copy, replace, or extend the canonical workflow here.
